---
phase: 04-pr2a-pwa-shell-manifest-sw
plan: 01
type: execute
wave: 1
depends_on: [03-01]
files_modified:
  - manifest.json                 # NEW
  - sw.js                         # NEW (~150 righe, separato da index.html — CON-010)
  - icons/icon-192.png            # NEW (favicon.io)
  - icons/icon-512.png            # NEW
  - icons/icon-512-maskable.png   # NEW (purpose=maskable, safe-zone 80%)
  - icons/icon-1024.png           # NEW
  - icons/apple-touch-icon-180.png# NEW (no transparency — iOS)
  - index.html                    # MODIFIED (<link rel=manifest> + meta theme-color + apple-touch-icon + apple meta + 3 <link rel=preload> + install-banner DOM, ~30 righe nette in)
  - app.js                        # MODIFIED (stale-SW filter+register, install-prompt state + helpers, UNDO-01 fix, SNAP-01 fix)
  - tests/sw.spec.ts              # MODIFIED (evolve REGRESSION-03 da swCount===0 a 1+scriptURL match)
  - tests/undo.spec.ts            # MODIFIED (rimuovere describe.skip riga 47)
  - tests/snapshot.spec.ts        # MODIFIED (rimuovere describe.skip riga 47)
  - tests/pwa-shell.spec.ts       # NEW (4 test: manifest, SW, banner non-visibile, banner-after-3-sessions, dismiss)
  - .github/workflows/playwright.yml  # MODIFIED (aggiungere paths-ignore: planning + md)
autonomous: false   # contiene T-LH (checkpoint manuale: Lighthouse audit ≥90 con screenshot) e T-PR (PR + squash-merge)
requirements:
  - REQ-PWA-01
  - REQ-PWA-02
  - REQ-PWA-03
  - REQ-SAFE-03    # carry-over PR1: SNAP-01 defensive priming chiude REQ-SAFE-03 in CI
  - REQ-SAFE-04    # carry-over PR1: UNDO-01 fix chiude la reattività calendario richiesta da REQ-SAFE-04
tags: [pwa, manifest, service-worker, install-prompt, lighthouse, carry-over-undo, carry-over-snap, ci-paths-ignore]
branch: pr2a-pwa-shell
user_setup: []   # nessun servizio esterno; favicon.io è generator one-shot offline-equivalent

must_haves:
  truths:
    - "Su Chrome desktop e Android Chrome (ovvero un browser che spara beforeinstallprompt) l'utente può fare Aggiungi a Home e l'app si apre standalone con icona 192/512 e theme color #0071e3"
    - "sw.js esiste come file separato (NON inline in index.html), implementa install (precache) / activate (cleanup caches !== CACHE_NAME + clients.claim) / fetch (SWR per app shell + 3 CDN pinnati, network-only per supabase.co|in, navigation con fallback a index.html cached); cache name è gestione-affitti-v1, bump manuale a ogni deploy che cambia precache"
    - "All'avvio dell'app, il loop di registrazione in app.js (righe 165–170 attuali) unregistra TUTTI i SW con scriptURL diverso da sw.js corrente e poi registra sw.js con scope './' — il SW corrente sopravvive, gli stale vengono ripuliti (CON-017 #3 resta verde nella nuova forma asserita)"
    - "Il banner di install (italiano: titolo 'Aggiungi alla schermata Home', CTA primary 'Aggiungi' su Chrome/Android, copy 'Tocca Condividi → Aggiungi a Home' su iOS Safari, CTA secondary 'Più tardi') appare SOLO quando: (a) installed flag non set AND (b) dismissed_until passato AND (c) session_log contiene ≥3 entry nel rolling 7d AND (d) o _isIosSafari o _deferredInstallPrompt catturato"
    - "Dismiss del banner persiste in localStorage.gestione_affitti_install_dismissed_until = ISO date di +14 giorni; il banner non riappare prima di quella data"
    - "Una nuova sessione viene registrata in localStorage.gestione_affitti_session_log SOLO se gap > 30 minuti dall'ultima entry; entry > 7 giorni vengono potate ad ogni recordSession"
    - "Detection iOS Safari usa /iPhone|iPad|iPod/.test(ua) OR (navigator.maxTouchPoints > 1 AND navigator.platform === 'MacIntel') per coprire iPadOS 13+ che si maschera come Mac, AND non già in standalone (matchMedia display-mode standalone || navigator.standalone)"
    - "Carry-over UNDO-01: gruppiCalendario() (app.js righe 958–979) usa filter inline su this.dati.proprieta/incassiAffitti con predicate '!x.deletedAt && …' invece di passare per this.attivi(); il test in tests/undo.spec.ts è un-skipped e diventa verde"
    - "Carry-over SNAP-01: salva() (app.js riga 707) ha defensive re-prime di this._lastSnapshotData = JSON.parse(JSON.stringify(this.dati)) se ancora null al momento della chiamata (opzione A di RESEARCH §8); il test in tests/snapshot.spec.ts è un-skipped e diventa verde"
    - "tests/pwa-shell.spec.ts esiste con 4 test (PWA-01 manifest+theme-color, PWA-02 sw.js registered + controller, PWA-03a banner-not-visible al primo accesso, PWA-03b banner-visible dopo iniezione 3 session + fake _deferredInstallPrompt, PWA-03c dismiss setta dismissed_until ~14d future) e gira verde in CI"
    - ".github/workflows/playwright.yml include paths-ignore: ['.planning/**', 'docs/**', '**.md'] su entrambi gli on.push e on.pull_request per evitare CI minute sprecate su commit planning-only"
    - "Lighthouse PWA score ≥ 90 sul preset mobile (`npx lighthouse https://oldpz.github.io/gestione-affitti/ --preset=desktop --view` OR Chrome DevTools Lighthouse mobile preset) — verificato manualmente dall'esecutore con screenshot allegato al PR body"
    - "Tutti i 13 Playwright spec esistenti (5 LOCKED regression + 8 LIVE) restano verdi; i 2 .skip carry-over diventano LIVE verdi; il nuovo pwa-shell.spec.ts è LIVE verde; CI verde sul PR prima del merge"
  artifacts:
    - path: "manifest.json"
      provides: "Web App Manifest: name=Gestione Affitti, short_name=Affitti, display=standalone, lang=it, start_url=./, scope=./, theme_color=#0071e3 (=PR0 --accent), background_color=#f5f5f7 (=PR0 --bg-base), icons 192/512/512-maskable/1024 + apple-touch-icon meta"
      contains: "\"display\": \"standalone\""
    - path: "sw.js"
      provides: "Service worker con install/activate/fetch handlers, CACHE_NAME='gestione-affitti-v1', PRECACHE_URLS (./index.html, ./app.js, ./manifest.json, 4 icone, 3 CDN), NETWORK_ONLY_HOSTS (supabase.co|in, fonts.googleapis.com), staleWhileRevalidate helper con guard su request.method!=='GET' + range header + status===200 prima del cache.put"
      contains: "const CACHE_NAME = 'gestione-affitti-v1';"
    - path: "icons/icon-192.png"
      provides: "192×192 PNG da favicon.io workflow Emoji-to-favicon 🏠 + gradient linear-gradient(135deg, #0071e3, #5e9eff) (matcha --gradient-logo di PR0), rounded square Sonoma-style, padding 12% safe-zone"
    - path: "icons/icon-512-maskable.png"
      provides: "512×512 PNG con safe-zone interna ≥80% (zoom-out o re-render favicon.io con maskable toggle) per non clippare l'emoji nelle silhouette Android"
    - path: "index.html"
      provides: "<link rel='manifest' href='manifest.json'> + <meta name='theme-color' content='#0071e3'> + 3 apple-mobile-web-app-* meta + <link rel='apple-touch-icon' href='icons/apple-touch-icon-180.png'> + 2 <link rel='icon'> + 3 <link rel='preload' as='script' href='...cdn...' crossorigin> per Tailwind/Supabase/Alpine + DOM banner install (.glass-card position:fixed bottom, x-show='installPromptVisible', riusa .btn-primary/.btn-secondary, data-testid install-banner/install-confirm/install-dismiss)"
      contains: "<link rel=\"manifest\" href=\"manifest.json\">"
    - path: "app.js"
      provides: "Sostituzione righe 165–170 con stale-SW filter-by-scriptURL + registrazione sw.js scope './'; nuovo state slot (installPromptVisible, _deferredInstallPrompt, _isIosSafari) + 4 helper (recordSession, maybeShowInstallBanner, installApp, dismissInstallPrompt) + listener beforeinstallprompt+appinstalled in init(); UNDO-01 fix in gruppiCalendario (3 inline filter); SNAP-01 fix in salva() (defensive priming 3 righe try/catch)"
      contains: "navigator.serviceWorker.register"
    - path: "tests/sw.spec.ts"
      provides: "REGRESSION-03 evoluto: asserisce regs.length === 1 AND scriptURL match /sw\\.js$/ (NON più swCount===0). Pattern: getRegistrations su page.evaluate dopo waitForLoadState networkidle."
    - path: "tests/pwa-shell.spec.ts"
      provides: "4 test PWA: PWA-01 manifest link + theme-color content match #0071e3; PWA-02 navigator.serviceWorker.controller non null + scriptURL /sw\\.js$/; PWA-03a banner not-visible default; PWA-03b inietta 3 session ISO + fake _deferredInstallPrompt via Alpine root._x_dataStack[0] + reload + assert banner visible; PWA-03c dismiss click + assert dismissed_until > now + 13d"
    - path: ".github/workflows/playwright.yml"
      provides: "paths-ignore: ['.planning/**', 'docs/**', '**.md'] su on.push.branches e on.pull_request.branches; job 'test' invariato (required check)"
  key_links:
    - from: "index.html <head>"
      to: "manifest.json + icons/*"
      via: "<link rel='manifest'> + <link rel='apple-touch-icon'> + meta theme-color"
      pattern: "rel=\"manifest\""
    - from: "app.js init() stale-SW block"
      to: "sw.js"
      via: "navigator.serviceWorker.register('sw.js', { scope: './' }) dopo aver unregistrato solo SW con scriptURL !== SW_ABS"
      pattern: "navigator\\.serviceWorker\\.register"
    - from: "sw.js fetch handler"
      to: "CACHE_NAME 'gestione-affitti-v1'"
      via: "staleWhileRevalidate(request) → caches.open(CACHE_NAME).then(cache => cache.match(request) || fetch+put)"
      pattern: "gestione-affitti-v"
    - from: "window beforeinstallprompt listener"
      to: "this._deferredInstallPrompt + this.maybeShowInstallBanner()"
      via: "e.preventDefault() + cattura evento, poi check session_log threshold"
      pattern: "beforeinstallprompt"
    - from: "this.dismissInstallPrompt"
      to: "localStorage.gestione_affitti_install_dismissed_until"
      via: "ISO date now() + 14 * 86400000"
      pattern: "gestione_affitti_install_dismissed_until"
    - from: "gruppiCalendario() (UNDO-01 fix)"
      to: "this.dati.proprieta/incassiAffitti diretti"
      via: "(arr || []).filter(x => !x.deletedAt && …) inline, NON più this.attivi(arr).filter"
      pattern: "filter\\(.*!.*deletedAt"
    - from: "salva() (SNAP-01 fix)"
      to: "this._lastSnapshotData defensive re-prime"
      via: "if (!this._lastSnapshotData) try { this._lastSnapshotData = JSON.parse(JSON.stringify(this.dati)); } catch(_){} prima di pushSnapshot"
      pattern: "_lastSnapshotData"
    - from: "tests/pwa-shell.spec.ts PWA-03b"
      to: "Alpine root._x_dataStack[0]._deferredInstallPrompt fake injection"
      via: "page.evaluate inietta 3 ISO timestamp in session_log + fake event object con prompt/userChoice"
      pattern: "_deferredInstallPrompt"
---

<objective>
Implementare il **PWA shell installabile** per `gestione-affitti` come **PR2a** (branch `pr2a-pwa-shell`, singolo PR squash-merged contro `master`). La phase consegna 5 success criteria ROADMAP Phase 4 + 2 carry-over di PR1:

1. `manifest.json` + meta tags in `index.html` → installabile Chrome desktop + Android Chrome con icona + theme color del design system PR0.
2. `sw.js` file separato (~150 righe) con Stale-While-Revalidate su app shell + 3 CDN pinnati, cache versionata `gestione-affitti-v1`, cleanup all'`activate`, `skipWaiting()` + `clients.claim()`.
3. Loop stale-SW evoluto in `app.js` righe 165–170: filter by scriptURL prima di unregister (NON più "unregister all"), poi register sw.js scope './'. Preserva CON-017 regression #3, evolvendo l'asserzione del test.
4. Banner italiano custom "Aggiungi alla schermata Home" che riusa `.glass-card`/`.btn-primary` di PR0 (zero CSS globale nuovo), triggera dopo 3 sessioni in 7 giorni (gap-sessione 30min), dismissabile per 14 giorni, branch iOS Safari con istruzioni Condividi→Aggiungi a Home.
5. Lighthouse PWA score ≥ 90 mobile (checkpoint manuale con screenshot).

Più:
6. **Carry-over UNDO-01** (PR1 skip): fix di `gruppiCalendario()` con inline filter — chiude `gruppiCalendario` Alpine reactivity break. Un-skip `tests/undo.spec.ts`.
7. **Carry-over SNAP-01** (PR1 skip): defensive re-prime di `_lastSnapshotData` in `salva()` (opzione A RESEARCH §8). Un-skip `tests/snapshot.spec.ts`.
8. **CI hygiene**: aggiunta `paths-ignore` a `.github/workflows/playwright.yml` per skippare CI su commit planning-only — risolve OQ-1 (discrepanza CONTEXT.md vs workflow reale).
9. **`load-fast-enough-for-pwa` mitigation**: `<link rel="preload" as="script" crossorigin>` per i 3 CDN (Tailwind, Supabase, Alpine) per assicurare ≥90 anche su 3G simulato — risolve OQ-8.
10. Evolvere `tests/sw.spec.ts` REGRESSION-03 da `swCount === 0` a `regs.length === 1 && scriptURL match /sw\.js$/`. Aggiungere nuovo spec `tests/pwa-shell.spec.ts` (4 test cases).

Purpose: Approach B (DEC-002) richiede una PWA reale come fondazione per PR2b (sync per-entità) e PR3 (notifiche locali via SW). PR2a isola il "PWA shell" dal "schema migration" (DEC-004) per failure modes indipendenti — un bug SW non deve bloccare la migrazione e viceversa. I 2 carry-over PR1 vivono qui perché PR2a è il prossimo branch che tocca `app.js` e i test skip non possono aspettare Phase 5.

Output: `manifest.json` + `sw.js` (NEW), 5 icons in `icons/` (NEW), `index.html` (~30 righe in: head meta + banner DOM), `app.js` (~80 righe in: SW register + install-prompt state/helpers + 2 fix carry-over), `tests/pwa-shell.spec.ts` (NEW ~80 righe), 2 spec un-skipped, 1 spec (sw.spec.ts) evoluto, workflow CI tweak. Branch `pr2a-pwa-shell` con CI verde, PR aperta, Lighthouse screenshot ≥ 90 allegato al PR body, squash-merge su `master`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-pr2a-pwa-shell-manifest-sw/04-CONTEXT.md
@.planning/phases/04-pr2a-pwa-shell-manifest-sw/04-RESEARCH.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-pr1-data-safety-net/03-01-SUMMARY.md
@.planning/phases/02-pr0-apple-sonoma-redesign/02-01-SUMMARY.md
@index.html
@app.js
@sw.js
@manifest.json
@tests/sw.spec.ts
@tests/undo.spec.ts
@tests/snapshot.spec.ts
@tests/fixtures.ts
@.github/workflows/playwright.yml

<interfaces>
<!-- Ground-truth da RESEARCH §2 (codebase map con line number esatti). USA QUESTE: -->

app.js (1263 righe totali):
- _lastSnapshotData slot init a null            riga 123
- async init() apertura                          riga 137
- localStorage.removeItem cleanup chiave          riga 162  (buon vicino dove infilare register-SW)
- ** Stale-SW unregister loop (CON-017 #3) **    righe 165–170  ← TARGET DELLA MODIFICA T-SWREG
- Prima chiamata caricaDatiUtente (auth attiva)  riga 175
- attivi(arr) definizione                         righe 223–226
- cestinoItems() getter (verde)                  righe 230–245
- pushSnapshot(preState)                          righe 320–333
- snapshots() getter                              righe 334–340
- ripristinaSnapshot re-prime pattern             riga 437
- Priming _lastSnapshotData happy A               riga 633
- Priming _lastSnapshotData happy B               riga 657
- Finally fallback priming                        righe 683–691
- ** salva() entry + pushSnapshot wire **        righe 707–714  ← TARGET DELLA MODIFICA T-SNAP01
- ** gruppiCalendario() getter (3 attivi() call) **  righe 958–979  ← TARGET DELLA MODIFICA T-UNDO01

index.html (1391 righe totali):
- <html lang="it">                                riga 2     (manifest lang: 'it' matcha)
- Tailwind CDN sync (URL https://cdn.tailwindcss.com)  riga 17
- Supabase JS CDN sync (https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2)  riga 19
- Alpine.js CDN defer (https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js)  riga 21
- :root tokens apertura                            riga 43
- --bg-base #f5f5f7 (= manifest background_color) riga 45
- --accent #0071e3 (= manifest theme_color)       riga 64
- --gradient-logo (= icon gradient #0071e3 → #5e9eff)  riga 80
- .glass-card definizione                          righe 195–202
- .btn-primary / .btn-secondary                    righe 241–248
- @supports backdrop-filter fallback              righe 234–238

tests/ stato attuale (verificato 2026-05-18):
| File | Stato attuale | Azione PR2a |
|---|---|---|
| tests/sw.spec.ts | LIVE (asserisce swCount === 0) | EVOLVERE → length===1 + scriptURL match |
| tests/undo.spec.ts | describe.skip riga 47 | UN-SKIP (togliere ".skip") |
| tests/snapshot.spec.ts | describe.skip riga 47 | UN-SKIP (togliere ".skip") |
| tests/offline.spec.ts | test.skip (gating PR2b) | NON TOCCARE |
| 5 LOCKED + login + cestino + calendario + salute-dati | LIVE | NON TOCCARE |

.github/workflows/playwright.yml stato attuale (verificato):
- on.push.branches: [master]
- on.pull_request.branches: [master]
- ** NESSUN paths-ignore presente ** ← OQ-1 risolta: AGGIUNGERE in T-CI
- job name: 'test' (riga 11) — required check su master-protect

Costanti da introdurre (decisioni del planner per OQ-2/3/5/6):
- CACHE_NAME = 'gestione-affitti-v1'        (OQ-3: manuale, bump a v2 al prossimo deploy che cambia precache)
- SESSION_GAP_MS = 30 * 60 * 1000            (OQ-5: 30 min)
- INSTALL_DISMISS_DAYS = 14                  (OQ-2: 14 giorni)
- iOS detection regex + iPadOS 13+ fallback  (OQ-6: includere maxTouchPoints + 'MacIntel')

CDN URL da pre-cachare e pre-loadare (LITERAL, copiare da index.html righe 17/19/21):
- https://cdn.tailwindcss.com
- https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2
- https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js

Hosts NETWORK_ONLY (mai cachati) in sw.js:
- supabase.co
- supabase.in
- fonts.googleapis.com

Nuovi data-testid da introdurre (Cypress-style):
- [data-testid="install-banner"]
- [data-testid="install-confirm"]
- [data-testid="install-dismiss"]

Pattern di commit (allineato a PR0/PR1):
- feat(pr2a): … (manifest/sw/icons/install-prompt features)
- fix(pr2a,UNDO-01): … / fix(pr2a,SNAP-01): …  (carry-over PR1)
- test(pr2a): … (new + un-skip)
- chore(pr2a): … (CI workflow tweak)

Branch + merge flow (LOCKED):
- Branch: pr2a-pwa-shell (NON pr2a-pwa-shell-manifest-sw — il dir di planning è lungo ma il branch è corto)
- Required check: 'test' (job name in playwright.yml, NON workflow name "Playwright Tests")
- Merge: squash-merge to master via PR (NO direct push di codice a master)
</interfaces>
</context>

---

# 1. Goal

Trasformare `gestione-affitti` da "web app classica servita da GitHub Pages" a "PWA installabile con shell offline-capable, banner custom italiano, cache versionata robusta e zero regressione sui 13 Playwright spec attivi". Chiudere i 2 test PR1 carry-over (UNDO-01, SNAP-01) nello stesso branch perché tocca lo stesso file (`app.js`). Allineare il workflow CI alla realtà documentata (paths-ignore per planning commit). Tutto in single-PR squash-merged contro `master`, branch `pr2a-pwa-shell`, required check `test` verde, zero build step introdotto, zero CSS globale nuovo (il banner riusa il design system PR0).

# 2. Success criteria mapping (ROADMAP §Phase 4 → task)

| # | ROADMAP success criterion | Delivered by | Proved by |
|---|---------------------------|--------------|-----------|
| SC1 | Aggiungi a Home funziona su Chrome desktop + Android, app standalone con icona + theme color | T-ICN + T-MAN + T-HEAD | T-PWA (PWA-01 + PWA-02) + T-LH manuale (Lighthouse Installability) |
| SC2 | sw.js separato, SWR su shell + CDN pinnati, cache versionata, cleanup all'activate, skipWaiting + clients.claim | T-SW | T-PWA PWA-02 + T-SMOKE DevTools > Application > Service Workers |
| SC3 | Stale SW unregistrati al boot — CON-017 #3 verde nella nuova forma | T-SWREG + T-SWSPEC | T-SWSPEC (sw.spec.ts evoluto asserisce 1 + scriptURL match) |
| SC4 | Banner italiano "Aggiungi alla schermata Home" dopo 3 sessioni in 7 giorni, dismissabile per 14 giorni | T-INST-STATE + T-INST-UI + T-INST-IOS | T-PWA PWA-03a/b/c |
| SC5 | Lighthouse PWA ≥ 90 mobile | T-PRELOAD (mitigation load-fast-enough) + tutto sopra | T-LH checkpoint manuale (screenshot allegato al PR body) |
| Carry UNDO-01 | gruppiCalendario reactivity ripristinata | T-UNDO01 (inline filter) + T-UNDO01-TEST | tests/undo.spec.ts un-skipped diventa verde |
| Carry SNAP-01 | Snapshot priming defensive in salva() | T-SNAP01 + T-SNAP01-TEST | tests/snapshot.spec.ts un-skipped diventa verde |
| CI hygiene | paths-ignore su workflow per planning commit | T-CI | Push successivo su `.planning/**` NON triggera CI |

# 3. File touch map

| File | Azione | Rationale |
|------|--------|-----------|
| `manifest.json` | **CREATE** | REQ-PWA-01, recipe literal da RESEARCH §3. |
| `sw.js` | **CREATE** (~150 righe) | REQ-PWA-02 CON-010. File separato (NON inline). Copy-adapt da RESEARCH §4. |
| `icons/icon-192.png` | **CREATE** | Manifest min size Android. favicon.io workflow RESEARCH §11. |
| `icons/icon-512.png` | **CREATE** | Manifest min size splash. |
| `icons/icon-512-maskable.png` | **CREATE** | purpose=maskable, safe-zone 80% (Android shape compliance). |
| `icons/icon-1024.png` | **CREATE** | Future-proofing high-DPI. |
| `icons/apple-touch-icon-180.png` | **CREATE** | iOS home screen, NO transparency. |
| `index.html` | MODIFY | `<head>`: 7 nuove righe (link manifest + meta theme-color + 3 apple-mobile-web-app-* + apple-touch-icon + 2 icon link + 3 preload). Body root scope: ~20 righe banner install DOM dopo `<div x-data="app()">`. Net ~+30 righe. |
| `app.js` | MODIFY | Righe 165–170 → ~25 righe (stale-SW filter + register). Nuovo state slot (3 campi) + 4 helper (~60 righe). Listener beforeinstallprompt+appinstalled in init() (~10 righe). gruppiCalendario 3 filter inline (UNDO-01, 3 righe modificate). salva() defensive priming (SNAP-01, 3 righe aggiunte). Net ~+95 righe. |
| `tests/sw.spec.ts` | MODIFY | Evolvere REGRESSION-03 — asserzione cambia da swCount===0 a length===1 + scriptURL match /sw\.js$/. |
| `tests/undo.spec.ts` | MODIFY | Riga 47: rimuovere `.skip` da `test.describe.skip(`. |
| `tests/snapshot.spec.ts` | MODIFY | Riga 47: rimuovere `.skip` da `test.describe.skip(`. |
| `tests/pwa-shell.spec.ts` | **CREATE** (~80 righe) | 4 test PWA-01..03c. Pattern Alpine root._x_dataStack[0] injection per fake _deferredInstallPrompt (Playwright/Chromium non triggera beforeinstallprompt nativo). |
| `.github/workflows/playwright.yml` | MODIFY | Aggiungere `paths-ignore: ['.planning/**', 'docs/**', '**.md']` su entrambi push e pull_request. Job invariato. |

**Da NON toccare**: `playwright.config.ts`, `package.json`, `package-lock.json`, `README.md`, `TUTORIAL.md`, `tests/offline.spec.ts` (skip mantenuto, gating PR2b), `tests/login.spec.ts`, `tests/cestino.spec.ts`, `tests/calendario.spec.ts`, `tests/salute-dati.spec.ts`, `tests/fixtures.ts` (a meno che T-PWA non riveli necessità di helper — *Claude's discretion*).

# 4. Pre-flight notes (cosa l'esecutore deve sapere PRIMA del commit 1)

1. **Line number da RESEARCH §2** sono autoritativi. Se il file differisce, fermarsi e chiedere — il branch parte da master post-PR1 (commit `c91959c`), quindi `app.js` = 1263 righe, `index.html` = 1391 righe.

2. **OQ resolved nel plan (no ulteriori domande all'utente)**:
   - OQ-1 paths-ignore: SI', aggiungerlo in T-CI (`.planning/**` + `docs/**` + `**.md`).
   - OQ-2 N-day dismiss: **14 giorni**.
   - OQ-3 cache version: **`gestione-affitti-v1` manuale**, bump in commit di deploy.
   - OQ-4 auto-update toast: **deferred a PR3**, niente UI di "ricarica per aggiornare" qui.
   - OQ-5 session gap: **30 minuti**.
   - OQ-6 iOS detection: **UA regex + (maxTouchPoints > 1 && platform === 'MacIntel')** per coprire iPadOS 13+.
   - OQ-7 maskable safe-zone: **verificare manualmente in T-ICN dopo favicon.io**; se favicon.io non offre toggle maskable, re-render con padding extra 12–20% e committare come `icon-512-maskable.png`.
   - OQ-8 preload CDN: **SI'**, task dedicato T-PRELOAD.

3. **Design tokens da usare (literal, da index.html PR0)**:
   - `theme_color: "#0071e3"` (= `--accent`, riga 64)
   - `background_color: "#f5f5f7"` (= `--bg-base`, riga 45)
   - icon gradient: `linear-gradient(135deg, #0071e3, #5e9eff)` (= `--gradient-logo`, riga 80)

4. **CDN literal URL** (copiare da index.html righe 17/19/21, NON paraphrasare):
   - `https://cdn.tailwindcss.com`
   - `https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2`
   - `https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js`

5. **Sequenza di commit con CI gate intermedio**: dopo T-SW + T-SWREG + T-SWSPEC (commit 4–5) push e attendere CI verde PRIMA di proseguire con install-prompt — il SW è il rischio più alto (R2) e va isolato. Stessa cautela dopo T-UNDO01 + T-SNAP01 (i carry-over toccano logica esistente).

6. **iPhone non testabile direttamente in CI** — il smoke iOS è manuale (T-SMOKE). Banner copy iOS branch va verificato leggendo il DOM rendered, NON triggerando `beforeinstallprompt` (che non firea su Chromium headless).

7. **GH Pages subpath**: il sito vive su `https://oldpz.github.io/gestione-affitti/`. Tutti i path nel manifest e nel sw.js DEVONO essere relativi (`./`, `./index.html`, `icons/*`). Path assoluti `/` rompono lo scope SW (controllerebbe la root del dominio invece del sub-path).

# 5. Task list (layered A → N, 16 task)

> 16 task atomici. Stima totale ~3h50m (RESEARCH §13) — **escalation a utente se >5h**, cap 25 min/task standard, **35 min** ammesso solo per T-SW (SW debugging notoriously sticky). Ogni task = un commit; alcune squash-coupled (test fix + un-skip = 1 commit). Branch: `pr2a-pwa-shell`. Push intermedio e attesa CI verde dopo T-SWSPEC e dopo T-SNAP01-TEST.

---

### T-BL — Baseline: branch + sanity check

- **Layer**: pre-A
- **Type**: setup
- **Time cap**: 5 min
- **Files touched**: nessuno (branch op)
- **What to do**:
  1. Da `master` aggiornato (post-Phase-3, HEAD `c91959c` o successivo): `git checkout -b pr2a-pwa-shell`.
  2. `wc -l index.html app.js` → atteso 1391 + 1263. Se difforme, **STOP e chiedi**.
  3. `grep -n "gruppiCalendario" app.js` → atteso ~riga 958.
  4. `grep -n "salva()" app.js | head -3` → confermare riga 707 area.
  5. `npx playwright test` → tutti verdi prima di iniziare (con i 2 carry-over ancora skipped). Se rossi, fixare PRIMA (baseline issue, non PR2a).
- **Done when**: branch creato, suite verde su HEAD, line counts confermati.
- **Depends on**: niente.
- **Commit**: nessuno.

---

### T-CI — Aggiungere `paths-ignore` al workflow CI

- **Layer**: A — CI hygiene (atomico, no-risk)
- **Type**: chore (CI)
- **Time cap**: 10 min
- **Files touched**: `.github/workflows/playwright.yml`
- **What to do**:
  1. Modificare la sezione `on:` per aggiungere `paths-ignore`:
     ```yaml
     on:
       push:
         branches: [master]
         paths-ignore:
           - '.planning/**'
           - 'docs/**'
           - '**.md'
       pull_request:
         branches: [master]
         paths-ignore:
           - '.planning/**'
           - 'docs/**'
           - '**.md'
       workflow_dispatch:
     ```
  2. Verificare yaml syntax con `node -e "require('js-yaml').load(require('fs').readFileSync('.github/workflows/playwright.yml','utf8'))"` se js-yaml è installato, altrimenti visual review.
  3. Commit. PUSH (è il primo commit del branch).
- **Done when**: file salvato con paths-ignore in entrambi on.push e on.pull_request. CI gira lo stesso su questo commit (il file workflow stesso non è in paths-ignore — viene escluso solo `.planning/**`/`docs/**`/`**.md`).
- **Depends on**: T-BL.
- **Commit**: `chore(pr2a): add paths-ignore to playwright workflow`.

---

### T-ICN — Genera 5 icone via favicon.io + commit

- **Layer**: B — Asset prerequisites
- **Type**: chore (assets)
- **Time cap**: 20 min
- **Files touched**: `icons/icon-192.png`, `icons/icon-512.png`, `icons/icon-512-maskable.png`, `icons/icon-1024.png`, `icons/apple-touch-icon-180.png`
- **What to do**:
  1. Andare su `https://favicon.io/emoji-favicons/house` (Emoji: 🏠 `U+1F3E0`).
  2. Configurare: background shape = rounded square, gradient `linear-gradient(135deg, #0071e3, #5e9eff)` (matcha `--gradient-logo` PR0), padding 12% safe-zone.
  3. Scaricare bundle, estrarre. Rinominare/regenerare nelle 5 size richieste (RESEARCH §11 tabella). Per la maskable: se favicon.io non ha toggle dedicato, prendere la 512 e re-renderla con padding interno extra 18% (zoom-out content fino a safe-zone ≥80%).
  4. `apple-touch-icon-180.png` **deve avere sfondo opaco** (iOS aggiunge sfondo nero se transparency).
  5. Verificare manualmente in un image viewer: emoji ben centrata, gradient visibile, maskable variant ha cintura di sicurezza interna chiaramente più larga.
  6. `mkdir icons && mv … icons/`. Commit. Verifica `ls icons/` mostra esattamente i 5 PNG.
  7. (Fallback se favicon.io down: usare lo script HTML standalone di RESEARCH §11 con `<canvas>` + `roundRect` + emoji.)
- **Done when**: 5 PNG presenti in `icons/`, total size < 200 KB (PNG ottimizzati).
- **Depends on**: T-BL.
- **Commit**: `feat(pr2a): icons (192/512/512-maskable/1024 + apple-touch-180)`.

---

### T-MAN — Aggiungere `manifest.json`

- **Layer**: B — Asset prerequisites
- **Type**: feat (manifest)
- **Time cap**: 10 min
- **Files touched**: `manifest.json` (CREATE)
- **What to do**:
  1. Creare `manifest.json` alla root del repo con il payload literal di RESEARCH §3 (copia-incolla):
     ```json
     {
       "name": "Gestione Affitti",
       "short_name": "Affitti",
       "description": "Gestione affitti e utenze per piccoli proprietari",
       "lang": "it",
       "dir": "ltr",
       "start_url": "./",
       "scope": "./",
       "display": "standalone",
       "orientation": "any",
       "theme_color": "#0071e3",
       "background_color": "#f5f5f7",
       "categories": ["finance", "productivity"],
       "icons": [
         { "src": "icons/icon-192.png", "sizes": "192x192", "type": "image/png", "purpose": "any" },
         { "src": "icons/icon-512.png", "sizes": "512x512", "type": "image/png", "purpose": "any" },
         { "src": "icons/icon-512-maskable.png", "sizes": "512x512", "type": "image/png", "purpose": "maskable" },
         { "src": "icons/icon-1024.png", "sizes": "1024x1024", "type": "image/png", "purpose": "any" }
       ]
     }
     ```
  2. Verifica con `node -e "JSON.parse(require('fs').readFileSync('manifest.json'))"` (no errori).
- **Done when**: file esiste e parsa, theme/background combaciano con i token PR0.
- **Depends on**: T-ICN (icone referenziate devono esistere).
- **Commit**: `feat(pr2a): manifest.json with PR0 design tokens`.

---

### T-HEAD — `<link rel="manifest">` + meta + `<link rel="preload">` in `index.html`

- **Layer**: C — index.html head
- **Type**: feat (html head)
- **Time cap**: 15 min
- **Files touched**: `index.html` (head only, ~10 righe in)
- **What to do**:
  1. Subito DOPO `<title>` e PRIMA del Tailwind CDN (riga 17), inserire (RESEARCH §3):
     ```html
     <link rel="manifest" href="manifest.json">
     <meta name="theme-color" content="#0071e3">
     <meta name="apple-mobile-web-app-capable" content="yes">
     <meta name="apple-mobile-web-app-status-bar-style" content="default">
     <meta name="apple-mobile-web-app-title" content="Affitti">
     <link rel="apple-touch-icon" href="icons/apple-touch-icon-180.png">
     <link rel="icon" type="image/png" sizes="192x192" href="icons/icon-192.png">
     <link rel="icon" type="image/png" sizes="512x512" href="icons/icon-512.png">

     <!-- Preload 3 CDN pinnati per migliorare load-fast-enough-for-pwa Lighthouse audit (OQ-8) -->
     <link rel="preload" as="script" crossorigin href="https://cdn.tailwindcss.com">
     <link rel="preload" as="script" crossorigin href="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2">
     <link rel="preload" as="script" crossorigin href="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js">
     ```
  2. **NON** rimuovere o riordinare gli `<script>` esistenti — preload è solo hint, gli script reali partono come prima.
  3. `wc -l index.html` → atteso ~1402 (+11).
- **Done when**: grep `rel="manifest"` ritorna 1 match in index.html; CI è skipped (siamo dentro `.github/workflows/playwright.yml`-non-touched ma index.html è tracked normale → CI gira) e resta verde.
- **Depends on**: T-MAN, T-ICN.
- **Commit**: `feat(pr2a): manifest link + apple meta + CDN preload`.

---

### T-SW — Creare `sw.js` (~150 righe, SWR + versioned cache + cleanup)

- **Layer**: D — Service worker
- **Type**: feat (sw)
- **Time cap**: 35 min (escalation ammessa per SW)
- **Files touched**: `sw.js` (CREATE)
- **What to do**:
  1. Creare `sw.js` alla root del repo. Copiare il body integrale da RESEARCH §4 (literal recipe ~150 righe). Decisioni già locked dentro al recipe:
     - `CACHE_NAME = 'gestione-affitti-v1'`
     - `PRECACHE_URLS` include `./`, `./index.html`, `./app.js`, `./manifest.json`, le 4 icons (192/512/1024/apple-touch-180), e i 3 CDN literal.
     - `NETWORK_ONLY_HOSTS = ['supabase.co', 'supabase.in', 'fonts.googleapis.com']`.
     - `install` precache + `self.skipWaiting()`.
     - `activate` cleanup di tutte le cache `!== CACHE_NAME` + `self.clients.claim()`.
     - `fetch` handler: skip non-GET, skip NETWORK_ONLY_HOSTS, navigation → SWR con fallback `index.html`, app shell + pinned CDN → SWR, default → passthrough.
     - Helper `staleWhileRevalidate`: skip range request, skip non-200 da cache.put.
  2. **Non includere** scheduler/notifications/idb-keyval (deferred a PR2b/PR3).
  3. **Non includere** `serviceworker.js` legacy redirect (Claude's discretion: lasciar perdere, non c'è storia).
  4. `node -e "new Function(require('fs').readFileSync('sw.js','utf8'))"` per syntax check (no exec, solo parse).
- **Done when**: file esiste, parsa, contiene `CACHE_NAME`, `PRECACHE_URLS`, `skipWaiting`, `clients.claim`, `staleWhileRevalidate`. CI verde (non c'è ancora wiring, sw.js non è registrato).
- **Depends on**: T-MAN, T-ICN (precache URL referenziano questi).
- **Commit**: `feat(pr2a): sw.js stale-while-revalidate + versioned cache`.

---

### T-SWREG — Sostituire stale-SW unregister loop in `app.js` + registrare `sw.js`

- **Layer**: D — Service worker registration
- **Type**: feat (sw register)
- **Time cap**: 25 min
- **Files touched**: `app.js` (righe 165–170 → ~25 righe)
- **What to do**:
  1. Sostituire `app.js` righe 165–170 attuali (unregister-all) con il diff di RESEARCH §5 (filter-by-scriptURL + register):
     ```js
     if ('serviceWorker' in navigator) {
       try {
         const SW_URL = 'sw.js';
         const SW_ABS = new URL(SW_URL, location.href).href;
         const regs = await navigator.serviceWorker.getRegistrations();
         for (const r of regs) {
           const active = r.active || r.installing || r.waiting;
           const url = active && active.scriptURL ? active.scriptURL : '';
           if (url !== SW_ABS) {
             await r.unregister();
           }
         }
         navigator.serviceWorker.register(SW_URL, { scope: './' }).catch((e) => {
           console.warn('SW register failed:', e);
         });
       } catch (e) {
         console.warn('SW boot error:', e);
       }
     }
     ```
  2. La `register()` è fire-and-forget (no `await`) per non rallentare il boot.
  3. Verifica locale: `npx serve` su porta 3000, apri `http://localhost:3000`, DevTools > Application > Service Workers → `sw.js` registered + activated, scope `http://localhost:3000/`.
  4. Hard-refresh: la registrazione resta, no errori console.
- **Done when**: localmente il SW si registra, controlla la pagina, no stale-SW residui. **NON pushare ancora** — il test sw.spec.ts attuale (`swCount === 0`) fallirebbe. Il prossimo task evolve la spec.
- **Depends on**: T-SW.
- **Commit**: **NON committare ancora**, fare commit unico con T-SWSPEC sotto (same-commit fixture pattern, lezione PR1 #4).

---

### T-SWSPEC — Evolvere `tests/sw.spec.ts` (REGRESSION-03 nuova forma) + commit con T-SWREG

- **Layer**: D — SW test sync
- **Type**: test (regression evolution)
- **Time cap**: 20 min
- **Files touched**: `tests/sw.spec.ts` (MODIFY)
- **What to do**:
  1. Sostituire il test attuale (`swCount === 0`) con la versione RESEARCH §10:
     ```ts
     import { test, expect } from './fixtures';

     test('REGRESSION-03: stale SWs unregistered at boot, current SW survives', async ({ page }) => {
       await page.goto('/');
       await page.waitForLoadState('networkidle');
       // Aspetta che il SW corrente prenda controllo (clients.claim).
       await page.waitForFunction(() => navigator.serviceWorker.controller !== null, { timeout: 10_000 });

       const result = await page.evaluate(async () => {
         const regs = await navigator.serviceWorker.getRegistrations();
         return regs.map(r => (r.active || r.installing || r.waiting)?.scriptURL || null);
       });

       expect(result.length).toBe(1);
       expect(result[0]).toMatch(/sw\.js$/);
     });
     ```
  2. **Commit unico** con T-SWREG (un solo `git add app.js tests/sw.spec.ts && git commit`).
  3. Run `npx playwright test tests/sw.spec.ts` locale → verde.
  4. **PUSH ora** (primo punto di CI gate). Attendere CI verde prima di proseguire.
- **Done when**: `tests/sw.spec.ts` aggiornato, CI verde su questo commit, sw.spec.ts evoluto + register coupled atomically.
- **Depends on**: T-SWREG (stesso commit).
- **Commit**: `feat(pr2a): register sw.js + evolve REGRESSION-03 (stale filter by scriptURL)`.

---

### T-UNDO01 — Fix UNDO-01: inline filter in `gruppiCalendario()`

- **Layer**: E — Carry-over PR1
- **Type**: fix (carry-over)
- **Time cap**: 10 min
- **Files touched**: `app.js` (righe 958–979, 3 righe modificate)
- **What to do**:
  1. Applicare il diff di RESEARCH §7 a `gruppiCalendario()` getter:
     - Riga 960: `this.attivi(this.dati.incassiAffitti).filter(i => i.mese === mese)` → `(this.dati.incassiAffitti || []).filter(i => !i.deletedAt && i.mese === mese)`
     - Riga 967: `this.attivi(this.dati.proprieta).find(p => p.id === inc.proprietaId)` → `(this.dati.proprieta || []).find(p => !p.deletedAt && p.id === inc.proprietaId)`
     - Riga 972: `for (const prop of this.attivi(this.dati.proprieta))` → `for (const prop of (this.dati.proprieta || []).filter(p => !p.deletedAt))`
  2. **NON rimuovere** `this.attivi()` helper — resta usato dagli altri 14 call site.
  3. Run locale `npx serve` → naviga a Calendario, soft-delete un incasso (cestino), verifica che la card scivoli da "incassi" a "mancanti" senza reload.
- **Done when**: 3 righe modificate, `grep -c "this.attivi" app.js` diminuito di 3 (da 19 a 16 — la coverage REGRESSION-04 di PR1 era "≥18" sul totale ma il valore atteso post-PR2a può scendere; documenta nel SUMMARY).
- **Depends on**: T-SWSPEC (CI deve essere verde prima di proseguire).
- **Commit**: vedi T-UNDO01-TEST (squash).

---

### T-UNDO01-TEST — Un-skip `tests/undo.spec.ts` + commit unico con T-UNDO01

- **Layer**: E — Carry-over PR1
- **Type**: test (un-skip)
- **Time cap**: 5 min
- **Files touched**: `tests/undo.spec.ts` (riga 47)
- **What to do**:
  1. Aprire `tests/undo.spec.ts`, alla riga 47 cambiare `test.describe.skip('UNDO toast …'` in `test.describe('UNDO toast …'` (rimuovere `.skip`).
  2. Run `npx playwright test tests/undo.spec.ts` locale → atteso verde (con il fix T-UNDO01 applicato).
  3. **Commit unico** con T-UNDO01.
- **Done when**: `.skip` rimosso, test verde locale, **commit pushed**, CI verde.
- **Depends on**: T-UNDO01 (stesso commit).
- **Commit**: `fix(pr2a,UNDO-01): inline filter in gruppiCalendario + un-skip undo.spec`.

---

### T-SNAP01 — Fix SNAP-01: defensive priming in `salva()`

- **Layer**: E — Carry-over PR1
- **Type**: fix (carry-over)
- **Time cap**: 10 min
- **Files touched**: `app.js` (riga 707 area, ~5 righe aggiunte)
- **What to do**:
  1. Applicare il diff opzione A di RESEARCH §8 a `salva()`:
     ```diff
      salva() {
        if (!this.utente) return;
        this.statoSalvataggio = 'salvataggio';
     +  // Defensive priming SNAP-01 (PR2a): se _lastSnapshotData e' ancora null
     +  // al momento della prima salva() (race tra multipli caricaDatiUtente,
     +  // finally guard non eseguito, etc.) usa lo stato corrente come pre-state.
     +  // Garantisce >=1 setItem('gestione_affitti_snapshots') per salva utente.
     +  if (!this._lastSnapshotData) {
     +    try { this._lastSnapshotData = JSON.parse(JSON.stringify(this.dati)); } catch (_) {}
     +  }
        if (this._lastSnapshotData) {
          this.pushSnapshot(this._lastSnapshotData);
        }
     ```
  2. **NON rimuovere** il priming esistente in `caricaDatiUtente` (righe 633/657/683–691). Il fix è additive.
  3. **NON applicare opzione B** (rimozione guardia `!this._lastSnapshotData`) — è più rischiosa, opzione A copre tutti gli scenari.
- **Done when**: 5 righe (incluso try/catch) aggiunte sopra `if (this._lastSnapshotData)`.
- **Depends on**: T-UNDO01-TEST.
- **Commit**: vedi T-SNAP01-TEST (squash).

---

### T-SNAP01-TEST — Un-skip `tests/snapshot.spec.ts` + commit unico con T-SNAP01 + CI gate

- **Layer**: E — Carry-over PR1
- **Type**: test (un-skip)
- **Time cap**: 5 min
- **Files touched**: `tests/snapshot.spec.ts` (riga 47)
- **What to do**:
  1. Riga 47: rimuovere `.skip` da `test.describe.skip(`.
  2. Run `npx playwright test tests/snapshot.spec.ts` locale → atteso verde (con T-SNAP01 applicato).
  3. **Commit unico** con T-SNAP01.
  4. **PUSH ora** (secondo CI gate). Attendere CI verde prima di proseguire.
- **Done when**: `.skip` rimosso, snapshot.spec verde locale, **push** + **CI verde**.
- **Depends on**: T-SNAP01 (stesso commit).
- **Commit**: `fix(pr2a,SNAP-01): defensive priming in salva() + un-skip snapshot.spec`.

---

### T-INST-STATE — Install prompt: state slot + helper in `app.js`

- **Layer**: F — Install prompt logic
- **Type**: feat (install prompt)
- **Time cap**: 25 min
- **Files touched**: `app.js` (state object + 4 metodi + 2 listener in init())
- **What to do**:
  1. Aggiungere al return object di `function app()` (vicino agli altri state slot, RESEARCH §6):
     ```js
     installPromptVisible: false,
     _deferredInstallPrompt: null,
     _isIosSafari: false,
     ```
  2. Aggiungere i 4 metodi (literal da RESEARCH §6): `recordSession()`, `maybeShowInstallBanner()`, `installApp()`, `dismissInstallPrompt()`. Costante `INSTALL_DISMISS_DAYS = 14` inline in `dismissInstallPrompt`. `SESSION_GAP_MS = 30 * 60 * 1000` inline in `recordSession`.
  3. In `init()`, **dopo** il blocco SW register (T-SWREG, ~riga 190 dopo l'edit) e **prima** di `await this.caricaDatiUtente()`, inserire:
     ```js
     // PWA install prompt detection (PR2a REQ-PWA-03)
     const ua = navigator.userAgent;
     const isIosUA = /iPhone|iPad|iPod/.test(ua);
     const isIpadOS = navigator.maxTouchPoints > 1 && navigator.platform === 'MacIntel';
     const isStandalone = window.matchMedia('(display-mode: standalone)').matches
                       || navigator.standalone === true;
     this._isIosSafari = (isIosUA || isIpadOS) && !isStandalone;

     window.addEventListener('beforeinstallprompt', (e) => {
       e.preventDefault();
       this._deferredInstallPrompt = e;
       this.maybeShowInstallBanner();
     });
     window.addEventListener('appinstalled', () => {
       this.installPromptVisible = false;
       this._deferredInstallPrompt = null;
       try { localStorage.setItem('gestione_affitti_installed', '1'); } catch (_) {}
     });
     this.recordSession();
     this.maybeShowInstallBanner();
     ```
  4. **iPadOS branch (OQ-6)**: la riga `isIpadOS` aggiunge il check `maxTouchPoints > 1 && platform === 'MacIntel'` — RESEARCH §6 originale aveva solo regex UA, qui esteso per scope (decisione plan).
- **Done when**: 3 state slot + 4 helper + 2 listener + recordSession+maybeShow in init(). `grep -c "installPromptVisible\|_deferredInstallPrompt\|_isIosSafari" app.js` ≥ 10 (definizione + uso multipli).
- **Depends on**: T-SNAP01-TEST.
- **Commit**: `feat(pr2a): install prompt state + helpers + listeners`.

---

### T-INST-UI — Install banner HTML in `index.html`

- **Layer**: F — Install prompt UI
- **Type**: feat (banner ui)
- **Time cap**: 15 min
- **Files touched**: `index.html` (~20 righe in, dentro `<div x-data="app()">` root scope)
- **What to do**:
  1. Inserire il banner DOM (literal da RESEARCH §6) dentro `<div x-data="app()">` a root scope, sopra il primo modal (cerca pattern `data-testid="impostazioni-section"` o l'`undo-toast` di PR1, è la stessa zona):
     ```html
     <!-- Install prompt banner (PR2a REQ-PWA-03) -->
     <div
       x-show="installPromptVisible"
       x-cloak
       data-testid="install-banner"
       class="glass-card"
       style="position: fixed; left: 16px; right: 16px; bottom: 16px; z-index: var(--z-toast);
              padding: 16px; display: flex; gap: 12px; align-items: center; max-width: 520px; margin: 0 auto;"
     >
       <div style="flex: 1;">
         <div style="font-weight: 600; margin-bottom: 4px;">Aggiungi alla schermata Home</div>
         <div x-show="!_isIosSafari" style="font-size: 13px; color: var(--text-secondary);">
           Installa l'app per accedere più rapidamente, anche offline.
         </div>
         <div x-show="_isIosSafari" style="font-size: 13px; color: var(--text-secondary);">
           Tocca <strong>Condividi</strong> &rarr; <strong>Aggiungi a Home</strong>.
         </div>
       </div>
       <button
         x-show="!_isIosSafari"
         class="btn-primary"
         data-testid="install-confirm"
         @click="installApp()"
       >Aggiungi</button>
       <button
         class="btn-secondary"
         data-testid="install-dismiss"
         @click="dismissInstallPrompt()"
       >Più tardi</button>
     </div>
     ```
  2. **NESSUNA nuova regola CSS globale**. Riusa `.glass-card`, `.btn-primary`, `.btn-secondary` di PR0 + inline style per positioning. `var(--z-toast)` esiste già (definito in PR0 token set; se non esiste, usare `z-index: 9999`).
  3. Smoke locale: `npx serve` → DevTools console: `Alpine.$data(document.querySelector('[x-data]')).installPromptVisible = true` → banner appare. Refresh: scompare.
- **Done when**: grep `data-testid="install-banner"` ritorna 1 match; banner renderizza correttamente in DevTools.
- **Depends on**: T-INST-STATE.
- **Commit**: `feat(pr2a): install banner UI reusing PR0 design system`.

---

### T-INST-IOS — *NESSUN task separato*

> La logica iOS branch è già in T-INST-STATE (detection) + T-INST-UI (copy `x-show="_isIosSafari"`). Non serve task dedicato. Questo segnaposto serve solo a documentare che OQ-6 è chiusa nei task precedenti.

---

### T-PWA — Nuovo spec `tests/pwa-shell.spec.ts` (4 test)

- **Layer**: G — PWA test coverage
- **Type**: test (new spec)
- **Time cap**: 30 min
- **Files touched**: `tests/pwa-shell.spec.ts` (CREATE)
- **What to do**:
  1. Creare `tests/pwa-shell.spec.ts` con i 4 test di RESEARCH §10 (PWA-01, PWA-02, PWA-03a, PWA-03b, PWA-03c — sono 5 ma PWA-03a è banale, può stare insieme a PWA-03b se preferisci 4 test atomici). Suggested: PWA-01 (manifest+theme-color), PWA-02 (SW controller), PWA-03a/b (banner non-visible → visible after injection), PWA-03c (dismiss).
  2. **Pattern injection Alpine** per fake `_deferredInstallPrompt`:
     ```ts
     await page.evaluate(() => {
       const root = document.querySelector('[x-data]') as any;
       if (root && root._x_dataStack?.[0]) {
         root._x_dataStack[0]._deferredInstallPrompt = { prompt: () => {}, userChoice: Promise.resolve() };
         root._x_dataStack[0].maybeShowInstallBanner();
       }
     });
     ```
  3. **Caveat**: `beforeinstallprompt` NON firea in Chromium headless. Tutti i test che richiedono "banner visible" devono iniettare manualmente il fake event + chiamare `maybeShowInstallBanner()`. NON tentare `page.evaluate(() => window.dispatchEvent(new Event('beforeinstallprompt')))` — non funziona perché manca tutto il payload `prompt`/`userChoice`.
  4. Run `npx playwright test tests/pwa-shell.spec.ts` → tutti verdi locale.
- **Done when**: 4–5 test in pwa-shell.spec.ts, tutti verdi locale.
- **Depends on**: T-INST-UI.
- **Commit**: `test(pr2a): pwa-shell spec (manifest, sw, banner state-injection)`.

---

### T-SUITE — Full suite green locale + push + CI gate finale

- **Layer**: H — Gate
- **Type**: test (verification)
- **Time cap**: 15 min
- **Files touched**: nessuno (run only)
- **What to do**:
  1. `npx playwright test` → tutti i 14 spec verdi (5 LOCKED + login + calendario + cestino + salute-dati + sw-evoluto + undo-un-skipped + snapshot-un-skipped + pwa-shell + offline-skip-mantenuto).
  2. Se rosso: triage. Se UNDO-01 o SNAP-01 falliscono in CI ma verdi locale, considerare flakiness (rerun 1x). Se persistente, **STOP e escalate**.
  3. Push. Attendere CI verde sul branch.
- **Done when**: CI verde su tutto, no test in red, no flake apparente.
- **Depends on**: T-PWA.
- **Commit**: nessuno (solo push se ci sono modifiche pending).

---

### T-LH — **Checkpoint**: Lighthouse audit ≥ 90 manuale + screenshot

- **Layer**: I — Manual verification
- **Type**: checkpoint:human-verify (gate=blocking)
- **Time cap**: 20 min
- **Files touched**: nessuno (audit only) — screenshot allegato al PR body in T-PR
- **What to do (esecutore)**:
  1. `npx serve` su porta 3000 (production-like, NON file://).
  2. Aprire `http://localhost:3000` in Chrome (Incognito per evitare estensioni).
  3. DevTools > Lighthouse > **Mobile preset** + **Categories: PWA** (più Performance per sicurezza).
  4. Analyze. Score atteso **≥ 90** (target ROADMAP SC5).
  5. Se < 90: leggere l'audit list, identificare i red flag. I più probabili: `load-fast-enough-for-pwa` (CDN slow su 3G simulato — preload in T-HEAD dovrebbe averlo mitigato). Mitigation possibile: bump tempo throttling, o accettare se score Performance > 80 e gli altri PWA audit sono verdi.
  6. Salvare screenshot del report (PNG, full page o solo PWA section). Storage in `.planning/phases/04-pr2a-pwa-shell-manifest-sw/lighthouse-pwa-score.png` (ignorato da git? — se sì, allegarlo direttamente al PR body via GH UI drag&drop).
  7. **Smoke addizionale (RESEARCH §12)**:
     - DevTools > Application > Service Workers: `sw.js` registered + activated.
     - DevTools > Application > Manifest: zero errori, icons preview OK.
     - Network > Offline: reload → app shell carica da cache (no white page), login form visibile.
     - LocalStorage: `gestione_affitti_session_log` ha 1 entry dopo primo load.
     - Mock 3 session > 30 min apart in LocalStorage → reload → banner appare.
     - Click "Più tardi" → `gestione_affitti_install_dismissed_until` = ISO ~14d future.
- **Done when**: screenshot Lighthouse mostra PWA score ≥ 90; smoke checklist tutta verde; screenshot saved per allegato al PR.
- **Depends on**: T-SUITE.
- **Resume signal**: "Lighthouse ≥ 90 OK, screenshot saved at <path>" o descrivere ostacoli (se score < 90, escalation).
- **Commit**: nessuno (manual audit).

---

### T-PR — PR + squash-merge

- **Layer**: J — Ship
- **Type**: checkpoint:human-action (gate=blocking, ma orchestrabile via `gh`)
- **Time cap**: 10 min
- **Files touched**: nessuno (process)
- **What to do**:
  1. `gh pr create --base master --head pr2a-pwa-shell --title "feat(pr2a): PWA shell + 2 PR1 carry-over fixes" --body "..."`
  2. PR body include:
     - Linea sommario dei 5 ROADMAP success criteria.
     - "Carry-over from PR1: UNDO-01 + SNAP-01 un-skipped and green."
     - "CI: paths-ignore aggiunto al workflow (skip CI su .planning/**, docs/**, **.md)."
     - Screenshot Lighthouse PWA ≥ 90 (drag&drop nell'editor PR).
     - Smoke checklist (RESEARCH §12) checkata tutta verde.
  3. Attendere CI verde sulla PR (required check `test`).
  4. Squash-merge via UI GitHub OR `gh pr merge <number> --squash --delete-branch`.
  5. **NON direct-push code a master** (CON-018, lock di processo).
  6. Post-merge: verificare GH Pages deploy a `https://oldpz.github.io/gestione-affitti/` → manifest installable, banner non visibile al primo accesso, SW registered.
- **Done when**: PR squash-merged, branch deleted, CI verde su master post-merge, deploy verde.
- **Depends on**: T-LH.
- **Commit**: il squash commit di GitHub (auto-generato).

---

# 6. Wave structure (parallelization hint)

PR2a è essenzialmente sequenziale (1 branch + 1 PR squash-merge), ma alcune coppie di task possono essere lavorate in parallelo da un singolo esecutore che switcha contesto se la tool-chain lo permette:

| Wave | Task | Note |
|------|------|------|
| W0 | T-BL | Setup, blocking |
| W1 | T-CI | Atomico, indipendente — può essere fatto in qualsiasi momento prima di T-PR. Lo metto qui per chiuderlo subito. |
| W1' | T-ICN, T-MAN | **Parallelizzabili**: T-ICN è I/O esterno (favicon.io), T-MAN è file-edit puro. Mentre attendi i download di favicon.io, scrivi manifest.json. |
| W2 | T-HEAD | Dipende da T-MAN+T-ICN |
| W3 | T-SW | Dipende da T-MAN+T-ICN |
| W4 | T-SWREG + T-SWSPEC (SAME COMMIT, **CI GATE 1**) | Bundle obbligatorio, push e attendi CI |
| W5 | T-UNDO01 + T-UNDO01-TEST (SAME COMMIT) | Carry-over PR1 |
| W6 | T-SNAP01 + T-SNAP01-TEST (SAME COMMIT, **CI GATE 2**) | Carry-over PR1 + push + attendi CI |
| W7 | T-INST-STATE | Dipende da SW già registered |
| W8 | T-INST-UI | Dipende da T-INST-STATE |
| W9 | T-PWA | Dipende da T-INST-UI |
| W10 | T-SUITE | Gate finale CI |
| W11 | T-LH (checkpoint manuale) | Blocking human-verify |
| W12 | T-PR (squash-merge) | Ship |

# 7. Atomic commit plan (8–10 commit, allineato a PR0/PR1 cadence)

1. `chore(pr2a): add paths-ignore to playwright workflow` (T-CI)
2. `feat(pr2a): icons (192/512/512-maskable/1024 + apple-touch-180)` (T-ICN)
3. `feat(pr2a): manifest.json with PR0 design tokens` (T-MAN)
4. `feat(pr2a): manifest link + apple meta + CDN preload` (T-HEAD)
5. `feat(pr2a): sw.js stale-while-revalidate + versioned cache` (T-SW)
6. `feat(pr2a): register sw.js + evolve REGRESSION-03 (stale filter by scriptURL)` (T-SWREG + T-SWSPEC) — **CI GATE**
7. `fix(pr2a,UNDO-01): inline filter in gruppiCalendario + un-skip undo.spec` (T-UNDO01 + T-UNDO01-TEST)
8. `fix(pr2a,SNAP-01): defensive priming in salva() + un-skip snapshot.spec` (T-SNAP01 + T-SNAP01-TEST) — **CI GATE**
9. `feat(pr2a): install prompt state + helpers + listeners` (T-INST-STATE)
10. `feat(pr2a): install banner UI reusing PR0 design system` (T-INST-UI)
11. `test(pr2a): pwa-shell spec (manifest, sw, banner state-injection)` (T-PWA)

= **11 commit atomici** (5–6 più dei minimi 8 stimati da RESEARCH §13, ma le coppie squash mantengono fixture-update e fix appaiati: lezione PR1 #2 "small no-op companions can ride along" rispettata).

# 8. Risk register

| ID | Risk | Lik/Imp | Mitigation | Task ownership |
|----|------|---------|------------|----------------|
| **R-SW** (CON-017 #3) | Stale-SW unregister cambia da unregister-all a filter-by-scriptURL → REGRESSION-03 spec attuale (`swCount===0`) fallisce | Cert/High | Evolvere sw.spec.ts nello STESSO commit (T-SWREG + T-SWSPEC) — pattern "same-commit fixture update" da PR1 lesson #4 | T-SWSPEC |
| **R-CACHE** (R2 CEO plan) | SW cache stale dopo deploy successivo blocca login/regressioni | Med/High | CACHE_NAME `v1` con bump manuale documentato in commento alto sw.js; `skipWaiting + clients.claim` attiva nuovo SW al prossimo navigate; SWR + cleanup `activate` puliscono cache vecchie | T-SW + smoke T-LH |
| **R-SUPABASE** | SW serve Supabase API da cache → offline test (LOCKED #4) sembra "online" | Med/High | `NETWORK_ONLY_HOSTS = ['supabase.co', 'supabase.in']` in sw.js evita Supabase passthrough cache. Verifica esplicita: `tests/offline.spec.ts` resta skip (PR2b), ma altri spec login/cestino non vengono pestati. | T-SW |
| **R-LH-PERF** | Lighthouse `load-fast-enough-for-pwa` < 10s su 3G simulato | Med/Med | 3 `<link rel="preload">` per CDN pinnati (T-HEAD). Se ancora < 90, escalate o accetta con justification nel PR body. | T-HEAD + T-LH |
| **R-UNDO-LATENT** | Fix UNDO-01 non copre H1 (Alpine tracking attraverso helper) — il sintomo riappare in altri 14 call site di `attivi()` | Low/Med | RESEARCH §7 nota: `cestinoItems` non usa `attivi()` ed è verde. Gli altri 14 sono usati in flussi diversi (visualizzazione liste, totali) dove la reactivity sembra funzionare. Mitigation: smoke T-LH testa flussi principali. Se appare in PR2b, fix dedicato. | T-UNDO01 |
| **R-SNAP-LATENT** | Defensive priming opzione A maschera root cause (race tra multipli caricaDatiUtente) | Med/Low | Fix è additive only. Se opzione B (rimuovere guardia in `caricaDatiUtente`) fosse necessaria in futuro, è un'aggiunta clean. Accetto trade-off per chiusura skip in PR2a. | T-SNAP01 |
| **R-IOS** | iPadOS 13+ in modalità desktop si comporta come Mac → `_isIosSafari = false` e banner mostra prompt nativo (che non firea) → nessun banner | Low/Low | Detection esteso con `maxTouchPoints > 1 && platform === 'MacIntel'` (OQ-6). User target = iPhone famiglia, iPad uso secondario. Smoke iOS manuale facoltativo. | T-INST-STATE |
| **R-LOCKED-1/2/5** (login, calendario segnaIncasso, cestino restore) | SW serve `app.js` cached vecchio → DOM o handler diversi → spec fail | Low/High | Primo deploy SW, nessuna cache pre-esistente. CI gira contro localhost:3000 fresh-server (no cache reale). `clients.claim()` + SWR garantiscono nuovo SW al prossimo navigate. | tutti via T-SUITE |

**LOCKED tests #1–#5 (CON-017) mitigation summary**: zero modifiche di logica a login/calendario/cestino in PR2a (UNDO-01 fix tocca SOLO `gruppiCalendario`, non `cestinoItems`); SW non avvelena le Supabase request. Rischio reale: **basso**.

# 9. Time budget

Stima realistica (RESEARCH §13, ricalibrata):

| Wave | Task | Time |
|------|------|------|
| W0 | T-BL | 5 min |
| W1 | T-CI | 10 min |
| W1' | T-ICN | 20 min |
| W1' | T-MAN | 10 min |
| W2 | T-HEAD | 15 min |
| W3 | T-SW | 35 min (escalation) |
| W4 | T-SWREG + T-SWSPEC | 20 + 20 = 40 min |
| W5 | T-UNDO01 + T-UNDO01-TEST | 10 + 5 = 15 min |
| W6 | T-SNAP01 + T-SNAP01-TEST | 10 + 5 = 15 min |
| W7 | T-INST-STATE | 25 min |
| W8 | T-INST-UI | 15 min |
| W9 | T-PWA | 30 min |
| W10 | T-SUITE | 15 min |
| W11 | T-LH | 20 min (manual) |
| W12 | T-PR | 10 min |
| **Totale** | | **~280 min ≈ 4h40m** |

Con buffer del 20% (lezione PR1 #1: line-count estimates serve buffer) → **~5h30m worst-case**. Cap CON-020: 5–6h. **Compatibile**, ma se T-SW debugging va in over (40+ min), **escalate**.

# 10. Verification protocol (DoD checklist)

Da eseguire prima di T-PR:

```
□ npx playwright test → 5 LOCKED + 9 LIVE (8 PR1 + sw-evoluto + undo-un-skipped + snap-un-skipped + pwa-shell con 4-5 test) tutti verdi
□ wc -l index.html → ~1422 (1391 baseline + ~30 in: 11 head + ~20 banner)
□ wc -l app.js → ~1358 (1263 baseline + ~95 in)
□ wc -l sw.js → ~150
□ ls icons/ → 5 PNG (192/512/512-maskable/1024/apple-touch-180)
□ jq . manifest.json → parsa, theme_color "#0071e3"
□ grep -c "this.attivi" app.js → 16 (era 19 in PR1; UNDO-01 fix rimuove 3 call in gruppiCalendario)
□ grep -c "CACHE_NAME" sw.js → ≥ 3
□ grep -c "skipWaiting\|clients.claim" sw.js → 2
□ grep -c "data-testid=\"install-banner\"" index.html → 1
□ grep -c "rel=\"manifest\"" index.html → 1
□ grep "paths-ignore" .github/workflows/playwright.yml → 2 matches (push + pull_request)
□ Lighthouse PWA score ≥ 90 (mobile preset) — screenshot saved
□ DevTools > Application > Service Workers → sw.js registered + activated
□ DevTools > Application > Manifest → no errors, icons preview OK
□ Offline reload → app shell carica da cache, no white page
□ Mock 3 session log + reload → banner visibile
□ Click "Più tardi" → install_dismissed_until ≈ now + 14d
□ CI verde sul PR
```

# 11. Out of scope (confirmed not implemented in PR2a)

- ❌ Push notifications scheduler in SW (deferred a PR3)
- ❌ Offline mutation queue + idb-keyval (deferred a PR2b)
- ❌ Background Sync API (deferred a PR2b)
- ❌ Auto-update toast "Ricarica per nuova versione" (deferred a PR3, OQ-4)
- ❌ Per-entity schema migration (Phase 5)
- ❌ Dark mode toggle (out of scope project-wide)
- ❌ Cache version auto-bump da SHA/version (PR2a usa manuale `v1`, auto-bump possibile in PR2b)
- ❌ Soft-delete su utenze/banche (PR1 deliberatamente solo proprietà+incassi)
- ❌ Nuovo CSS globale (banner riusa esclusivamente classi PR0)
- ❌ ES modules / build step (CON-001 LOCKED)

# 12. Success criteria (DoD finale, copy-paste-able)

PR2a è **DONE** quando tutti i seguenti sono veri:

1. ✅ Su Chrome desktop + Android Chrome l'utente può "Aggiungi a Home" e l'app si apre standalone con icona 192/512 e theme color `#0071e3`.
2. ✅ `sw.js` esiste come file separato (~150 righe), implementa install/activate/fetch con SWR + cache versionata `gestione-affitti-v1` + cleanup all'`activate` + `skipWaiting`+`clients.claim`.
3. ✅ Stale SW da sessioni precedenti unregistrati al boot (filter-by-scriptURL, register `sw.js` scope `./`). `tests/sw.spec.ts` (REGRESSION-03 evoluto) verde.
4. ✅ Banner italiano "Aggiungi alla schermata Home" appare dopo 3 sessioni in 7 giorni (gap 30 min). Dismiss persiste 14 giorni. Branch iOS Safari mostra istruzioni Condividi → Aggiungi a Home.
5. ✅ Lighthouse PWA ≥ 90 mobile, screenshot allegato al PR body.
6. ✅ `tests/undo.spec.ts` un-skipped e verde (UNDO-01 fix in `gruppiCalendario`).
7. ✅ `tests/snapshot.spec.ts` un-skipped e verde (SNAP-01 defensive priming in `salva()`).
8. ✅ `tests/pwa-shell.spec.ts` esiste con 4–5 test, tutti verdi in CI.
9. ✅ `.github/workflows/playwright.yml` ha `paths-ignore` su push e pull_request.
10. ✅ Tutti i 5 LOCKED Playwright regression + 8 LIVE spec restano verdi (zero regression CON-017).
11. ✅ CI verde sul PR; squash-merged a master; GH Pages deploy verde.
12. ✅ Zero nuovo CSS globale (banner riusa `.glass-card`/`.btn-primary`/`.btn-secondary` di PR0).
13. ✅ Zero build step introdotto (CON-001).

<verification>
Run `npx playwright test` post-merge su master e verificare 5 LOCKED + 9 LIVE tutti verdi.
Run Lighthouse mobile preset su https://oldpz.github.io/gestione-affitti/ post-deploy: PWA ≥ 90.
DevTools Application tab su prod URL: Service Workers attivo, Manifest senza errori.
</verification>

<success_criteria>
Vedi §12.
</success_criteria>

<output>
After completion, create `.planning/phases/04-pr2a-pwa-shell-manifest-sw/04-01-SUMMARY.md` con:
- Tasks executed (commit map) come tabella
- Plan deviations
- Line counts finali (index.html, app.js, sw.js)
- Verification protocol results
- Lighthouse score effettivo + path screenshot
- Patterns established (riusabili per PR2b/PR3 — es. SW versioning workflow, install prompt pattern)
- Risk register actual outcomes
- Lessons learned
- Out of scope confirmed
- Next steps (PR2b kickoff hint)
</output>
