---
plan_id: 05-05
phase: 5
title: Playwright E2E suite + CRITICAL-05 LIVE + PR + merge
wave: 4
depends_on: [05-01, 05-02, 05-03, 05-04]
files_modified:
  - tests/fixtures.ts                # MODIFIED (~80 righe: MOCK_DATI extension + goOffline/goOnline helpers + per-entity seed via service key)
  - tests/sync-offline.spec.ts       # MODIFIED (era offline.spec.ts: rimuovi .skip, full CRITICAL-05 LIVE)
  - tests/conflict.spec.ts           # NEW (~100 righe)
  - tests/migration.spec.ts          # NEW (~140 righe — idempotency + partial-fail)
  - tests/inquilini.spec.ts          # NEW (~70 righe — CRUD + cestino)
  - tests/tipi-utenza.spec.ts        # NEW (~60 righe — add + reactive dropdown + delete blocked)
  - tests/scadenza.spec.ts           # NEW (~50 righe — scadenzaGiorno + calendar grouping)
  - app.js                           # MODIFIED (~10 righe: debug globals window._mutationQueueDebug + window._conflictQueueDebug behind localhost gate)
autonomous: false
requirements: [REQ-SYNC-01, REQ-SYNC-02, REQ-SCHEMA-01, REQ-DATA-01, REQ-DATA-02, REQ-DATA-03]
estimated_hours: 4.5
---

# Plan 05-05: Playwright E2E suite + CRITICAL-05 LIVE + PR + merge

**Phase 5 — PR2b** | Wave 4 | Depends on: 05-01, 05-02, 05-03, 05-04
**Files modified:** `tests/fixtures.ts`, `tests/sync-offline.spec.ts`, `tests/conflict.spec.ts` (NEW), `tests/migration.spec.ts` (NEW), `tests/inquilini.spec.ts` (NEW), `tests/tipi-utenza.spec.ts` (NEW), `tests/scadenza.spec.ts` (NEW), `app.js` (debug globals)
**Requirements:** REQ-SYNC-01, REQ-SYNC-02, REQ-SCHEMA-01, REQ-DATA-01, REQ-DATA-02, REQ-DATA-03 (verification coverage cross-reqs)
**Est:** 4.5h

## Goal
Coprire i 6 success criteria ROADMAP con E2E Playwright: CRITICAL-05 LIVE (CON-017 gate finalmente verde), conflict resolution end-to-end, migration idempotency + partial-fail, inquilini CRUD, tipi_utenza reactive dropdown, scadenzaGiorno calendar grouping. Aprire PR e portarlo a merge.

## Context summary
- CRITICAL-05 era `test.skip` da Phase 1 PR5 (gating PR2b). Rimuoverlo è il **gate finale** che chiude CON-017.
- `setOffline` Playwright + SW quirk: serve manual dispatch `await page.evaluate(() => window.dispatchEvent(new Event('online')))` come supplemento (RESEARCH §9 + R-NEW-2).
- Debug globals `window._mutationQueueDebug` e `window._conflictQueueDebug` esposti SOLO in dev/test (localhost or `window.__PLAYWRIGHT_TEST__` flag).
- Test conflict: seed mutation in IDB via `page.evaluate(() => idbKeyval.set(...))`, bump server `updated_at` via service key, online → conflict toast.
- Test migration: seed blob legacy via service key, login → assert tables popolate + JSON download + idempotent re-run.
- Test migration partial-fail: seed blob con FK orfana → modal blocking + emergency export.
- `tests/fixtures.ts` MOCK_DATI estesa con `tipi_utenza` seed, `inquilini: []`, `scadenzaGiorno` su ogni proprietà; service-key trigger DB esteso per consentire INSERT/UPDATE/DELETE su tutte le 6 nuove tabelle per TEST_USER_ID.

## must_haves
- [ ] `tests/sync-offline.spec.ts` LIVE (`.skip` rimosso): airplane mode → 3 write (inquilino + incasso + utenza) → online → queue drains in FK order → server consistent
- [ ] `tests/conflict.spec.ts` passa: seed 409 via service-key updated_at bump → modal "Conflitto su [nome]" → "Usa cloud" → state locale = server; rerun "Usa mia" → server = local
- [ ] `tests/migration.spec.ts` passa: first-run popola tabelle + scarica JSON; second-run = no-op (no download); partial-fail (FK orfana) → modal `migration-failed-modal` + emergency download
- [ ] `tests/inquilini.spec.ts` passa: create/edit/delete/restore inquilino
- [ ] `tests/tipi-utenza.spec.ts` passa: aggiungi "rifiuti" → appare in dropdown utenza senza reload; hard-delete tipo usato → toast bloccato
- [ ] `tests/scadenza.spec.ts` passa: crea proprietà scadenzaGiorno=17 → card in sezione calendario "Giorno 17"
- [ ] **Tutti i 19 spec esistenti restano verdi** (13 di PR1/PR2a + 6 nuovi PR2b) — full CI verde
- [ ] PR branch `pr2b-schema-migration-sync` aperto contro master con CI verde; squash-merged

## Tasks

### T-05-05-01: Debug globals in `app.js` (test-only)

<task id="T-05-05-01" wave="1" depends_on="" type="execute">
  <title>Esporre `window._mutationQueueDebug` e `_conflictQueueDebug` (localhost only)</title>
  <read_first>
    - app.js riga 148-273 (init body)
    - .planning/phases/05-pr2b-schema-migration-sync/05-RESEARCH.md §9 (queue inspection helper)
  </read_first>
  <action>
    In `init()` di `app()`, dopo il boot completo (vicino alla fine, prima del return implicito), aggiungere:

    ```js
    // Debug globals (LOCALHOST/TEST ONLY) — usato da Playwright per inspect queue/conflict state
    if (location.hostname === 'localhost' || location.hostname === '127.0.0.1' || window.__PLAYWRIGHT_TEST__) {
      window._mutationQueueDebug = () => this.mutationQueue;
      window._conflictQueueDebug = () => this.conflictQueue;
      window._appInstance = this; // per page.evaluate avanzato
    }
    ```

    NON esporre in production (la guard su hostname è sufficiente per GH Pages che serve da `oldpz.github.io`).
  </action>
  <acceptance_criteria>
    - `grep "_mutationQueueDebug" app.js` ritorna ≥ 1
    - `grep "_conflictQueueDebug" app.js` ritorna ≥ 1
    - `grep "location.hostname === 'localhost'" app.js` ritorna ≥ 1
    - commit: `chore(pr2b): debug globals for Playwright queue inspection`
  </acceptance_criteria>
</task>

### T-05-05-02: `tests/fixtures.ts` extension (MOCK_DATI + helpers + service-key seed)

<task id="T-05-05-02" wave="1" depends_on="" type="execute">
  <title>Extend fixtures: MOCK_DATI new entities + goOffline/goOnline + 6-table seed via service key</title>
  <read_first>
    - tests/fixtures.ts (esistente)
    - .planning/phases/05-pr2b-schema-migration-sync/05-RESEARCH.md §9 (offline lifecycle helper + fixture update)
  </read_first>
  <action>
    1. **MOCK_DATI extension** in `fixtures.ts`:
       ```ts
       export const MOCK_DATI = {
         dataVersion: 4,
         tipi_utenza: [
           { id: 'tu-acqua-test', nome: 'acqua' },
           { id: 'tu-luce-test', nome: 'luce' },
           { id: 'tu-gas-test', nome: 'gas' },
         ],
         banche: [/* esistenti, invariati */],
         proprieta: [
           // ogni proprietà esistente: aggiungi scadenzaGiorno: '1' (o variato per scadenza.spec.ts)
         ],
         inquilini: [],
         incassiAffitti: [/* esistenti */],
         utenze: [/* esistenti — assicurarsi che `tipo` legacy esista; mapping a tipo_id avviene in migration */],
       };
       ```

    2. **Helpers offline/online**:
       ```ts
       import { Page } from '@playwright/test';

       export async function goOffline(page: Page) {
         await page.context().setOffline(true);
         await page.evaluate(() => window.dispatchEvent(new Event('offline')));
       }
       export async function goOnline(page: Page) {
         await page.context().setOffline(false);
         await page.evaluate(() => window.dispatchEvent(new Event('online')));
       }

       export async function waitForQueueDrain(page: Page, timeoutMs = 10_000) {
         await page.waitForFunction(() => (window._mutationQueueDebug?.() || []).length === 0, { timeout: timeoutMs });
       }
       ```

    3. **Service-key seed function** (`seedFreshTestUser()` esistente — estendere):
       Aggiungere chiamate Supabase service-key per:
       - Pulire le 6 nuove tabelle: `delete from <table> where user_id = TEST_USER_ID` per ognuna (in ordine inverso FK: utenze → incassi_affitti → inquilini → proprieta → banche → tipi_utenza).
       - Inserire i seed di MOCK_DATI nelle 6 tabelle nuove (manualmente in ordine FK: tipi_utenza prima, poi banche, ecc.).
       - Mantenere il `dati_utente.blob_json` set come safety per i test legacy che ancora usano blob path.

       Codice (estensione del fixture esistente):
       ```ts
       // Dentro seedFreshTestUser, dopo il blob seed esistente:
       const SERVICE_SB = createClient(SUPABASE_URL, SERVICE_KEY);
       // Pulisci in ordine FK-aware
       for (const t of ['utenze', 'incassi_affitti', 'inquilini', 'proprieta', 'banche', 'tipi_utenza']) {
         await SERVICE_SB.from(t).delete().eq('user_id', TEST_USER_ID);
       }
       // Insert tipi_utenza
       await SERVICE_SB.from('tipi_utenza').insert(MOCK_DATI.tipi_utenza.map(t => ({ ...t, user_id: TEST_USER_ID })));
       // ... idem per altre tabelle, mappando camelCase → snake_case
       ```

    4. **DB trigger extension**: se il fixture esistente fa uso di un trigger DB / RPC dedicato per il seed (verificare con `grep -n "seedFreshTestUser\|reset_test_data" tests/`), estenderlo per coprire le 6 tabelle nuove (RESEARCH §9 nota "estendere il trigger DB"). Se non c'è trigger custom (seed via service key diretto), no-op.
  </action>
  <acceptance_criteria>
    - `grep "tipi_utenza:" tests/fixtures.ts` ritorna ≥ 1
    - `grep "export async function goOffline" tests/fixtures.ts` ritorna 1
    - `grep "export async function goOnline" tests/fixtures.ts` ritorna 1
    - `grep "waitForQueueDrain" tests/fixtures.ts` ritorna ≥ 1
    - `grep -c "from('tipi_utenza')\\|from('inquilini')" tests/fixtures.ts` ≥ 2 (seed coverage)
    - commit: `test(pr2b): fixtures extension — MOCK_DATI + offline helpers + per-entity seed`
  </acceptance_criteria>
</task>

### T-05-05-03: `tests/sync-offline.spec.ts` — CRITICAL-05 LIVE

<task id="T-05-05-03" wave="2" depends_on="T-05-05-01,T-05-05-02" type="execute">
  <title>Un-skip + completare CRITICAL-05 (airplane mode → 3 writes FK-aware → online → consistent)</title>
  <read_first>
    - tests/offline.spec.ts (esistente, currently .skip)
    - .planning/phases/05-pr2b-schema-migration-sync/05-RESEARCH.md §9 sezione 1 (CRITICAL-05 LIVE spec)
  </read_first>
  <action>
    1. **Rename** `tests/offline.spec.ts` → `tests/sync-offline.spec.ts` (`git mv`).

    2. **Rimuovere** `.skip` dal `test.describe.skip(...)` o `test.skip(...)`.

    3. **Riscrivere body** con scenario CRITICAL-05 LIVE:
       ```ts
       import { test, expect } from '@playwright/test';
       import { goOffline, goOnline, waitForQueueDrain, seedFreshTestUser, SUPABASE_URL, SERVICE_KEY, TEST_USER_ID } from './fixtures';
       import { createClient } from '@supabase/supabase-js';

       test.describe('CRITICAL-05 (CON-017): offline write + sync FK-aware', () => {
         test.beforeEach(async ({ page }) => {
           await seedFreshTestUser();
           await page.goto('/');
           // ... login flow tramite TEST_USER credentials
         });

         test('3 write in airplane mode → online → queue drains in FK topological order', async ({ page }) => {
           // 1. Vai a Impostazioni → Inquilini → aggiungi "Mario Test"
           // 2. Vai a Calendario → segna un incasso che usa una proprietà esistente
           // 3. Vai a Utenze → crea utenza tipo "acqua" sulla stessa proprietà

           // PRIMA: assicurati di essere online e poi setOffline
           await goOffline(page);
           await expect(page.locator('[data-testid="queue-indicator"]')).not.toBeVisible(); // ancora 0 entry

           // Add inquilino
           await page.locator('[data-testid="inquilino-new"]').click();
           await page.locator('[data-testid="inquilino-nome"]').fill('Mario Test');
           await page.locator('[data-testid="inquilino-cf"]').fill('RSSMRA80A01H501Z');
           await page.locator('[data-testid="inquilino-proprieta"]').selectOption({ index: 1 });
           await page.locator('button[type="submit"]:has-text("Salva")').click();

           // Add incasso (semplificato — usa il pattern del cestino.spec.ts esistente)
           // ... interagisci con form incasso

           // Add utenza tipo acqua
           // ... interagisci con form utenza

           // Verify queue indicator
           await expect(page.locator('[data-testid="queue-indicator"]')).toContainText(/\d+ modifiche in coda/);
           const queueLen = await page.evaluate(() => window._mutationQueueDebug().length);
           expect(queueLen).toBeGreaterThanOrEqual(3);

           // Verify ordine FK in queue prima del flush
           const entries = await page.evaluate(() => window._mutationQueueDebug());
           // Sort manualmente con stesso algoritmo e verifica che le insert siano nell'ordine giusto
           // (inquilini deve venir prima di incassi/utenze; questo è il critical assertion)

           // 4. Torna online → flush triggered
           await goOnline(page);
           await waitForQueueDrain(page);
           await expect(page.locator('[data-testid="queue-indicator"]')).not.toBeVisible();

           // 5. Verifica server-side via service key
           const sb = createClient(SUPABASE_URL, SERVICE_KEY);
           const { data: inq } = await sb.from('inquilini').select('*').eq('user_id', TEST_USER_ID).eq('nome', 'Mario Test');
           expect(inq).toHaveLength(1);
           const { data: inc } = await sb.from('incassi_affitti').select('*').eq('user_id', TEST_USER_ID);
           expect(inc.length).toBeGreaterThan(0);
           const { data: ut } = await sb.from('utenze').select('*').eq('user_id', TEST_USER_ID);
           expect(ut.length).toBeGreaterThan(0);
         });
       });
       ```

    4. Note: se `setOffline` Playwright si dimostra non-affidabile in CI (R-NEW-2), aggiungere `await page.evaluate(() => window.dispatchEvent(new Event('online')))` manuale dopo `goOnline`. Documentare nel commit.
  </action>
  <acceptance_criteria>
    - file `tests/sync-offline.spec.ts` esiste; `tests/offline.spec.ts` NON esiste
    - `grep "test.skip\\|test.describe.skip" tests/sync-offline.spec.ts` ritorna 0
    - `grep "CRITICAL-05" tests/sync-offline.spec.ts` ritorna ≥ 1
    - `grep "waitForQueueDrain" tests/sync-offline.spec.ts` ritorna ≥ 1
    - `npx playwright test tests/sync-offline.spec.ts` locale → verde (skip se Supabase test project non disponibile sul dev box; verde su CI)
    - commit: `test(pr2b,CRITICAL-05): un-skip offline sync — LIVE E2E`
  </acceptance_criteria>
</task>

### T-05-05-04: `tests/conflict.spec.ts` (NEW)

<task id="T-05-05-04" wave="2" depends_on="T-05-05-01,T-05-05-02" type="execute">
  <title>Conflict 409 → modal binary choose (locale|cloud) end-to-end</title>
  <read_first>
    - .planning/phases/05-pr2b-schema-migration-sync/05-RESEARCH.md §9 sezione 2 (conflict.spec.ts spec)
  </read_first>
  <action>
    Creare `tests/conflict.spec.ts`:

    ```ts
    import { test, expect } from '@playwright/test';
    import { goOffline, goOnline, seedFreshTestUser, SUPABASE_URL, SERVICE_KEY, TEST_USER_ID } from './fixtures';
    import { createClient } from '@supabase/supabase-js';

    test.describe('REQ-SYNC-02: per-entity conflict resolution (binary choose)', () => {
      test.beforeEach(async ({ page }) => {
        await seedFreshTestUser();
        await page.goto('/');
        // login as TEST_USER_ID
      });

      test('409 → toast "Conflitto su" → "Usa cloud" applies server payload', async ({ page }) => {
        // Pre-condition: una proprietà esistente. Prendi il primo id.
        const propId = await page.evaluate(() => window._appInstance.dati.proprieta[0].id);
        const propNome = await page.evaluate(() => window._appInstance.dati.proprieta[0].nome);

        // 1. Simula edit offline (push direct mutation in queue)
        await goOffline(page);
        await page.evaluate(([id]) => {
          window._appInstance.mutationQueue.push({
            op: 'update', entity: 'proprieta', id,
            payload: { nome: 'Locale Wins', updated_at: new Date(Date.now() - 60000).toISOString() },
            ts: Date.now(), attempts: 0,
            baseUpdatedAt: new Date(Date.now() - 60000).toISOString(),
          });
          window._appInstance._mutationQueueVersion++;
        }, [propId]);

        // 2. Server-side: aggiorna updated_at "futuro" via service key
        const sb = createClient(SUPABASE_URL, SERVICE_KEY);
        await sb.from('proprieta').update({ nome: 'Cloud Wins', updated_at: new Date(Date.now() + 60000).toISOString() }).eq('id', propId);

        // 3. Online → flush → 409 → conflict toast
        await goOnline(page);
        await expect(page.locator('[data-testid="conflict-toast"]')).toBeVisible({ timeout: 10_000 });
        await expect(page.locator('[data-testid="conflict-toast"]')).toContainText(/Conflitto su/);

        // 4. Click "Usa versione cloud"
        await page.locator('[data-testid="conflict-keep-server"]').click();
        await expect(page.locator('[data-testid="conflict-toast"]')).not.toBeVisible();

        // 5. Verifica stato locale = cloud
        const nomeLocale = await page.evaluate(([id]) => window._appInstance.dati.proprieta.find(p => p.id === id)?.nome, [propId]);
        expect(nomeLocale).toBe('Cloud Wins');
      });

      test('409 → "Usa la mia versione" force-updates server', async ({ page }) => {
        // Mirror flow ma click conflict-keep-local
        const propId = await page.evaluate(() => window._appInstance.dati.proprieta[0].id);

        await goOffline(page);
        await page.evaluate(([id]) => {
          window._appInstance.mutationQueue.push({
            op: 'update', entity: 'proprieta', id,
            payload: { nome: 'Locale Wins After Conflict', updated_at: new Date().toISOString() },
            ts: Date.now(), attempts: 0,
            baseUpdatedAt: new Date(Date.now() - 60000).toISOString(),
          });
          window._appInstance._mutationQueueVersion++;
        }, [propId]);

        const sb = createClient(SUPABASE_URL, SERVICE_KEY);
        await sb.from('proprieta').update({ nome: 'Cloud Will Lose', updated_at: new Date(Date.now() + 60000).toISOString() }).eq('id', propId);

        await goOnline(page);
        await expect(page.locator('[data-testid="conflict-toast"]')).toBeVisible({ timeout: 10_000 });
        await page.locator('[data-testid="conflict-keep-local"]').click();
        await expect(page.locator('[data-testid="conflict-toast"]')).not.toBeVisible();

        // Verify server-side via service key
        const { data } = await sb.from('proprieta').select('nome').eq('id', propId).single();
        expect(data.nome).toBe('Locale Wins After Conflict');
      });
    });
    ```
  </action>
  <acceptance_criteria>
    - file `tests/conflict.spec.ts` esiste
    - `grep "data-testid=\"conflict-toast\"" tests/conflict.spec.ts` ritorna ≥ 2
    - `grep "data-testid=\"conflict-keep-server\"" tests/conflict.spec.ts` ritorna 1
    - `grep "data-testid=\"conflict-keep-local\"" tests/conflict.spec.ts` ritorna 1
    - 2 test passano in CI
    - commit: `test(pr2b,REQ-SYNC-02): conflict resolution E2E (locale + cloud branches)`
  </acceptance_criteria>
</task>

### T-05-05-05: `tests/migration.spec.ts` (NEW — idempotency + partial-fail)

<task id="T-05-05-05" wave="2" depends_on="T-05-05-01,T-05-05-02" type="execute">
  <title>Migration: first-run, idempotency (second-run no-op), partial-fail modal</title>
  <read_first>
    - .planning/phases/05-pr2b-schema-migration-sync/05-RESEARCH.md §9 sezione 3 (migration.spec.ts spec)
  </read_first>
  <action>
    Creare `tests/migration.spec.ts` con 3 test:

    ```ts
    import { test, expect } from '@playwright/test';
    import { seedFreshTestUser, SUPABASE_URL, SERVICE_KEY, TEST_USER_ID } from './fixtures';
    import { createClient } from '@supabase/supabase-js';

    test.describe('REQ-SCHEMA-01: blob → per-entity migration (idempotent + safe-fail)', () => {
      test('first-run migrates blob to entities + downloads JSON backup', async ({ page }) => {
        // Setup: blob popolato, tabelle nuove vuote
        const sb = createClient(SUPABASE_URL, SERVICE_KEY);
        for (const t of ['utenze', 'incassi_affitti', 'inquilini', 'proprieta', 'banche', 'tipi_utenza']) {
          await sb.from(t).delete().eq('user_id', TEST_USER_ID);
        }
        await seedFreshTestUser(); // popola solo blob

        const downloadPromise = page.waitForEvent('download', { timeout: 15_000 });
        await page.goto('/');
        // login flow → triggers caricaDatiUtente → triggers migraBlobAPerEntita

        const download = await downloadPromise;
        expect(download.suggestedFilename()).toMatch(/gestione-affitti.*\.json/);

        // Verify post-migration: tabelle popolate
        const { count: pCount } = await sb.from('proprieta').select('id', { count: 'exact', head: true }).eq('user_id', TEST_USER_ID);
        expect(pCount).toBeGreaterThan(0);
      });

      test('second-run is no-op (idempotent: no migration, no download)', async ({ page }) => {
        // Pre-condition: tabelle già popolate (previous test left them)
        let downloadFired = false;
        page.on('download', () => { downloadFired = true; });

        await page.goto('/');
        // login flow
        await page.waitForLoadState('networkidle');

        expect(downloadFired).toBe(false);
      });

      test('partial-fail (orphan FK) → modal + emergency export', async ({ page }) => {
        const sb = createClient(SUPABASE_URL, SERVICE_KEY);
        // Reset: tabelle nuove vuote, blob con FK orfana
        for (const t of ['utenze', 'incassi_affitti', 'inquilini', 'proprieta', 'banche', 'tipi_utenza']) {
          await sb.from(t).delete().eq('user_id', TEST_USER_ID);
        }
        // Seed blob con incasso che punta a proprieta non esistente
        await sb.from('dati_utente').upsert({
          user_id: TEST_USER_ID,
          blob_json: {
            dataVersion: 3,
            banche: [], proprieta: [],
            incassiAffitti: [{ id: 'inc-orfano', proprietaId: '00000000-0000-0000-0000-000000000000', mese: '2026-05', importo: 100 }],
            utenze: [], tipi_utenza: [], inquilini: [],
          },
        });

        await page.goto('/');
        // login

        await expect(page.locator('[data-testid="migration-failed-modal"]')).toBeVisible({ timeout: 15_000 });

        const downloadPromise = page.waitForEvent('download', { timeout: 10_000 });
        await page.locator('[data-testid="migration-emergency-export"]').click();
        const download = await downloadPromise;
        expect(download.suggestedFilename()).toMatch(/gestione-affitti.*\.json/);
      });
    });
    ```
  </action>
  <acceptance_criteria>
    - file `tests/migration.spec.ts` esiste
    - `grep "page.waitForEvent('download'" tests/migration.spec.ts` ritorna ≥ 2
    - `grep "data-testid=\"migration-failed-modal\"" tests/migration.spec.ts` ritorna 1
    - `grep "data-testid=\"migration-emergency-export\"" tests/migration.spec.ts` ritorna 1
    - 3 test passano in CI
    - commit: `test(pr2b,REQ-SCHEMA-01): migration idempotency + partial-fail`
  </acceptance_criteria>
</task>

### T-05-05-06: `tests/inquilini.spec.ts` + `tests/tipi-utenza.spec.ts` + `tests/scadenza.spec.ts`

<task id="T-05-05-06" wave="2" depends_on="T-05-05-01,T-05-05-02" type="execute">
  <title>3 spec UX nuove entità — CRUD inquilini, tipi_utenza reactive, scadenzaGiorno grouping</title>
  <read_first>
    - .planning/phases/05-pr2b-schema-migration-sync/05-RESEARCH.md §9 sezioni 4/5/6
    - tests/cestino.spec.ts (pattern per soft-delete + ripristina)
  </read_first>
  <action>
    1. **`tests/inquilini.spec.ts`** (~70 righe):
       ```ts
       import { test, expect } from '@playwright/test';
       import { seedFreshTestUser } from './fixtures';

       test.describe('REQ-DATA-01: inquilini CRUD', () => {
         test.beforeEach(async ({ page }) => { await seedFreshTestUser(); await page.goto('/'); /* login */ });

         test('create + edit + soft-delete + ripristina', async ({ page }) => {
           await page.locator('text=Impostazioni').click();
           await page.locator('[data-testid="inquilino-new"]').click();
           await page.locator('[data-testid="inquilino-nome"]').fill('Test Inquilino');
           await page.locator('[data-testid="inquilino-cf"]').fill('TSTQ80A01H501Z');
           await page.locator('button:has-text("Salva")').first().click();
           await expect(page.locator('[data-testid="inquilini-section"]')).toContainText('Test Inquilino');

           // Edit
           await page.locator('[data-testid="inquilino-edit"]').first().click();
           await page.locator('[data-testid="inquilino-nome"]').fill('Modificato');
           await page.locator('button:has-text("Salva")').first().click();
           await expect(page.locator('[data-testid="inquilini-section"]')).toContainText('Modificato');

           // Soft-delete
           await page.locator('[data-testid="inquilino-delete"]').first().click();
           await expect(page.locator('[data-testid="inquilini-section"]')).not.toContainText('Modificato');

           // Verify in cestino + ripristina (pattern PR1)
           await page.locator('text=Cestino').click();
           await expect(page.locator('text=Modificato')).toBeVisible();
           await page.locator('button:has-text("Ripristina")').first().click();
           // ... assert visibile nuovamente in Inquilini
         });
       });
       ```

    2. **`tests/tipi-utenza.spec.ts`** (~60 righe):
       ```ts
       test.describe('REQ-DATA-02: tipi_utenza dinamici', () => {
         test('aggiungi "rifiuti" → appare immediatamente in dropdown utenza', async ({ page }) => {
           // navigate to Impostazioni → Tipi utenza
           await page.locator('[data-testid="tipo-new-input"]').fill('rifiuti');
           await page.locator('[data-testid="tipo-new-submit"]').click();
           await expect(page.locator('[data-testid="tipi-utenza-section"]')).toContainText('rifiuti');

           // Naviga a Utenze → form nuova utenza
           await page.locator('text=Utenze').click();
           await page.locator('button:has-text("Nuova utenza")').click();
           const optionTexts = await page.locator('[data-testid="form-utenza-tipo"] option').allTextContents();
           expect(optionTexts).toContain('rifiuti');
         });

         test('hard-delete tipo_utenza con utenze associate → bloccato', async ({ page }) => {
           // pre: c'è già un'utenza tipo "acqua" (da MOCK_DATI)
           await page.locator('text=Impostazioni').click();
           // bottone delete tipo "acqua" deve essere disabled (data-testid="tipo-delete")
           const acquaDelete = page.locator('[data-testid="tipo-delete"]').first();
           // Verifica disabled state via attribute
           await expect(acquaDelete).toBeDisabled();
         });
       });
       ```

    3. **`tests/scadenza.spec.ts`** (~50 righe):
       ```ts
       test.describe('REQ-DATA-03: scadenzaGiorno custom + calendar grouping', () => {
         test('crea proprietà con scadenzaGiorno=17 → appare in sezione "Giorno 17"', async ({ page }) => {
           await page.locator('text=Proprietà').click();
           await page.locator('button:has-text("Nuova proprietà")').click();
           await page.locator('input[placeholder*="Nome"]').first().fill('Test Scadenza 17');
           await page.locator('[data-testid="form-scadenza-giorno"]').selectOption('17');
           await page.locator('button[type="submit"]:has-text("Salva")').click();

           await page.locator('text=Calendario').click();
           await expect(page.locator('text=Giorno 17')).toBeVisible();
           // ... verifica che la card "Test Scadenza 17" sia sotto quella sezione
         });
       });
       ```
  </action>
  <acceptance_criteria>
    - 3 file esistono in `tests/`
    - tutti e 3 superano `npx playwright test` in CI
    - commit (3 separati o 1 combinato): `test(pr2b,REQ-DATA-01/02/03): inquilini + tipi_utenza + scadenza E2E`
  </acceptance_criteria>
</task>

### T-05-05-07: [CI-GATE] Push branch + verde + apri PR

<task id="T-05-05-07" wave="3" depends_on="T-05-05-03,T-05-05-04,T-05-05-05,T-05-05-06" type="execute">
  <title>Branch push, CI verde, PR creation</title>
  <read_first>
    - .github/workflows/playwright.yml (CI workflow esistente)
  </read_first>
  <action>
    1. Verificare branch corrente è `pr2b-schema-migration-sync` (creato all'inizio della Phase 5; se non esiste: `git checkout -b pr2b-schema-migration-sync`).
    2. `git push -u origin pr2b-schema-migration-sync`.
    3. Attendere CI run. Se fallisce, diagnosticare:
       - Test Supabase project non disponibile in CI → check env vars (SUPABASE_URL, SERVICE_KEY, TEST_USER_ID/PASSWORD).
       - Playwright setOffline flaky → applicare manual dispatch (vedi T-05-05-03 note).
       - Schema non applicato in CI test project → manual apply schema files (T-05-01-04, T-05-03-01) anche su test project Supabase.

    4. **Aprire PR** con `gh pr create`:
       ```
       gh pr create --title "feat(pr2b): schema migration + per-entity sync" --body "$(cat <<'EOF'
       ## Summary
       Phase 5 — PR2b. Migra il salvataggio da blob singolo a tabelle per-entità con sync offline FK-aware, conflict resolution binary, e 3 nuove entità (inquilini, tipi_utenza, scadenzaGiorno).

       ## Implementa
       - REQ-SCHEMA-01: blob → 6 per-entity tables; RPC `get_user_data()` single-roundtrip; migration idempotente atomic PL/pgSQL; rollback flag UI
       - REQ-SYNC-01: mutation queue idb-keyval FK-aware; 3× retry; "N modifiche in coda" indicator
       - REQ-SYNC-02: conflict toast binary (locale | cloud), non-dismissible
       - REQ-DATA-01: inquilini CRUD + cestino integration
       - REQ-DATA-02: tipi_utenza CRUD dinamici (dropdown reattivo)
       - REQ-DATA-03: scadenzaGiorno per proprietà + calendar grouping
       - CRITICAL-05 LIVE (CON-017 chiuso)

       ## Schema migrations applied to prod
       - 20260520_pr2b_schema.sql
       - 20260520_pr2b_get_user_data.sql
       - 20260520_pr2b_migrate_blob.sql

       ## Test plan
       - [x] All 13 PR1/PR2a spec restano verdi
       - [x] sync-offline.spec.ts LIVE (CRITICAL-05)
       - [x] conflict.spec.ts (2 test)
       - [x] migration.spec.ts (3 test idempotency + partial-fail)
       - [x] inquilini.spec.ts / tipi-utenza.spec.ts / scadenza.spec.ts

       ## Dual-write window
       Phase 1 attiva: app legge da nuovo schema, scrive a entrambi (per-entity + blob).
       Phase 3 (rimozione blob fallback) → +2 settimane, deploy successivo.

       ## Rollback
       `localStorage.usaNuovoSchema = 'false'` (UI toggle in Salute dati → Avanzato).

       🤖 Generated with Claude Code
       EOF
       )"
       ```

    5. Squash-merge dopo CI verde + review (manuale).
  </action>
  <acceptance_criteria>
    - branch `pr2b-schema-migration-sync` pushed
    - CI run su PR è verde (job `test` passes)
    - PR aperto contro `master`
    - PR body contiene il template sopra
    - **DOPO MERGE (out of scope task ma documentato)**: deploy automatico GH Pages, verifica `https://oldpz.github.io/gestione-affitti/` carica e fa get_user_data; smoke manuale 5 min su mobile reale
  </acceptance_criteria>
</task>

## Verification
- `npx playwright test` locale (ambiente connesso a Supabase test project) → 19 spec passano (13 vecchi + 6 nuovi PR2b).
- Sulla PR aperta, CI status check `test` è verde.
- `gh pr view --json mergeable` → `MERGEABLE`.

## Threat model
- **Test isolation: state leak fra spec**: ogni `beforeEach` chiama `seedFreshTestUser()` che pulisce le 6 tabelle nuove + blob via service key. Verifica T-05-05-02 step 3 pulisce in ordine FK inverso (no constraint violation).
- **Service key in CI env**: SERVICE_KEY è in GitHub Secrets (esistente da PR5). Mai loggata; mai committata in repo. Verifica `grep -r "SERVICE_KEY" tests/` mostra solo riferimenti tramite env var, non literal.
- **Debug globals shipped to prod**: T-05-05-01 ha guard `location.hostname === 'localhost'`. GH Pages serve da `oldpz.github.io` → guard false → globals undefined. Manual verify in DevTools prod: `window._mutationQueueDebug` deve essere `undefined`.
- **Race in conflict spec**: il test setta server `updated_at` via service key tra goOffline e goOnline; service key è "out-of-band" e non rispetta RLS. Verifica che l'aggiornamento sia visibile dal client (read-after-write consistency Supabase: ms-level).

## Risks & mitigations
- **CI flakiness su Playwright setOffline + SW**: documentato R-NEW-2; manual dispatch fallback già nel test. Se persiste in CI, deferire CRITICAL-05 a un retry decorator `test.describe.configure({ retries: 2 })` — accettato come ultima risorsa.
- **Test parallel execution + shared TEST_USER_ID**: il fixture `seedFreshTestUser` non è isolation-safe se 2 spec girano in parallelo. Mitigation: `playwright.config.ts` ha già `workers: 1` (PR5 setup). Verificare; se non lo è, settare `--workers=1` in CI.
- **Migration test "first-run" depends on test execution order**: i 3 test `migration.spec.ts` hanno dipendenza implicita (second-run aspetta tabelle popolate da first-run). Mitigation: usare `test.describe.serial` o un beforeEach più aggressivo che resetta a stato precis. Accettato come trade-off di clarity (RESEARCH §9 sezione 3).

---

*Plan 05-05 of phase 05-pr2b-schema-migration-sync*
