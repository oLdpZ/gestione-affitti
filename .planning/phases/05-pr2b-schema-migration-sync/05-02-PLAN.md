---
plan_id: 05-02
phase: 5
title: Mutation queue (FK-aware) + idb-keyval + conflict UI (per-entity write path)
wave: 2
depends_on: [05-01]
files_modified:
  - app.js                        # MODIFIED (~250 righe: state queue/conflict, flushMutationQueue, applyMutation, sortQueue, enqueueConflict, risolviConflitto, nomeUmano, salva() branch dual-write)
  - index.html                    # MODIFIED (~40 righe: conflict toast modal in root x-data, queue-indicator badge)
autonomous: true
requirements: [REQ-SYNC-01, REQ-SYNC-02]
estimated_hours: 4.5
---

# Plan 05-02: Mutation queue FK-aware + idb-keyval + conflict UI

**Phase 5 — PR2b** | Wave 2 | Depends on: 05-01
**Files modified:** `app.js`, `index.html`
**Requirements:** REQ-SYNC-01, REQ-SYNC-02
**Est:** 4.5h

## Goal
Implementare il **write path per-entità**: enqueue mutazioni in IndexedDB via `idb-keyval`, flush in ordine topologico FK al ritorno online (3× retry su 5xx, 409 → conflict UI binary, 401 → refresh + retry trasparente), toast persistente "N modifiche in coda", modal conflict non-dismissible. Wave 2: dipende dal foundation di 05-01 ma è indipendente da migration/UX nuove entità (plan 05-03/05-04 procedono in parallelo).

## Context summary
- Mutation entry shape: `{ op, entity, id, payload, ts, attempts, baseUpdatedAt }` (RESEARCH §5).
- Topological order **hardcoded** (falsifica Kahn's): `['tipi_utenza', 'banche', 'proprieta', 'inquilini', 'incassi_affitti', 'utenze']` (RESEARCH §5).
- Flush algoritmo: `while queue.length > 0` { sort + applyMutation(head) }, re-entrancy guard `_flushing`, retry 3× con backoff `[1s, 3s, 9s]` su 5xx, 409 → enqueueConflict + skip, 401 → refreshSession + continue (RESEARCH §5).
- Conflict queue FIFO, una modal visibile alla volta — copy italiano locked CONTEXT linea 162-167.
- Dual-write Phase 1 (DEC-012): durante questo PR ogni mutazione scrive **sia** entry per-entity (queue) **che** blob legacy via `salvaSubitoBlob()` come safety net. Blob removal viene a Phase 3 (+2 settimane).
- Alpine reactivity per `mutationQueueCount()` e `conflittoCorrente()` via counter slots `_mutationQueueVersion` / `_conflictVersion` (pattern PR2a `_snapshotVersion`, wiki/concepts/alpine-reactive-trigger-for-external-source.md).

## must_haves
- [ ] State Alpine: `mutationQueue`, `_mutationQueueVersion`, `conflictQueue`, `_conflictVersion`, `_flushing` esistono in `app()`
- [ ] Al boot: `init()` legge `mutationQueue` da `idbKeyval.get('mutationQueue')` (default `[]`)
- [ ] `window.addEventListener('online', ...)` chiama `flushMutationQueue()` PRIMA di `caricaDatiUtente()`
- [ ] `flushMutationQueue()` rispetta ordine FK hardcoded, retry 3× su 5xx (backoff `[1000,3000,9000]`), 409 → `enqueueConflict`, 401 → `refreshSession`
- [ ] `salva()` branched: se `usaNuovoSchema` → enqueue per-entity + dual-write blob legacy; altrimenti percorso blob esistente intatto
- [ ] Conflict modal in root `<div x-data="app()">` con bottoni "Usa la mia versione" e "Usa versione cloud", `data-testid="conflict-toast"`/`conflict-keep-local`/`conflict-keep-server`, non-dismissible (no click-overlay, no Esc)
- [ ] Toast indicator `<div x-show="mutationQueueCount() > 0 && !_isOnline">` con testo "N modifiche in coda"

## Tasks

### T-05-02-01: State slots + idb-keyval boot load

<task id="T-05-02-01" wave="1" depends_on="" type="execute">
  <title>State queue/conflict + boot load da `idbKeyval.get('mutationQueue')`</title>
  <read_first>
    - app.js riga 110-150 (state slots area) + riga 148-273 (init body)
    - .planning/phases/05-pr2b-schema-migration-sync/05-RESEARCH.md §4 (idb-keyval API) + §5 (state shape)
  </read_first>
  <action>
    1. **State slots** in `app()` return object (vicino agli slot di 05-01):
       ```js
       mutationQueue: [],
       _mutationQueueVersion: 0,
       conflictQueue: [],
       _conflictVersion: 0,
       _flushing: false,
       ```

    2. **Boot load** in `init()` (dopo Supabase setup ~riga 148, prima di `caricaDatiUtente()`):
       ```js
       // PR2b mutation queue restore (REQ-SYNC-01)
       try {
         if (typeof idbKeyval !== 'undefined') {
           this.mutationQueue = (await idbKeyval.get('mutationQueue')) || [];
           this._mutationQueueVersion++;
         }
       } catch (e) {
         // Private browsing / IDB disabled → fallback in-memory; documented R-NEW-1
         this.mutationQueue = [];
         this.pushErrore({ message: 'idbKeyval boot: ' + (e?.message || 'unknown'), severity: 'warn' });
       }
       ```

    3. **Alpine reactive getter** `mutationQueueCount()`:
       ```js
       mutationQueueCount() {
         void this._mutationQueueVersion;
         return this.mutationQueue.length;
       },
       conflittoCorrente() {
         void this._conflictVersion;
         return this.conflictQueue[0] || null;
       },
       ```
  </action>
  <acceptance_criteria>
    - `grep "mutationQueue: \[\]" app.js` ritorna 1
    - `grep "conflictQueue: \[\]" app.js` ritorna 1
    - `grep "idbKeyval.get('mutationQueue')" app.js` ritorna 1
    - `grep "mutationQueueCount()" app.js` ritorna ≥ 2 (definizione + uso template più tardi)
    - smoke locale: console `app._mutationQueueDebug = () => app().mutationQueue` accessibile (esposto debug in plan 05-05)
    - commit: `feat(pr2b): mutation queue state + idb-keyval boot load`
  </acceptance_criteria>
</task>

### T-05-02-02: `sortQueue()` + `enqueueMutation()` + `applyMutation()` helpers

<task id="T-05-02-02" wave="2" depends_on="T-05-02-01" type="execute">
  <title>Helper FK-aware sort + enqueue + applyMutation (single network call)</title>
  <read_first>
    - .planning/phases/05-pr2b-schema-migration-sync/05-RESEARCH.md §5 (sortQueue + flush + applyMutation contract)
    - .planning/phases/05-pr2b-schema-migration-sync/05-CONTEXT.md linea 51 (ordine topologico locked)
  </read_first>
  <action>
    Aggiungere metodi a `app()`:

    ```js
    // FK-aware topological order (LOCKED — hardcoded falsifica Kahn, vedi RESEARCH §5)
    // Se in futuro una self-FK viene aggiunta, sostituire con Kahn's algorithm.
    // ENTITY_FLUSH_ORDER definito come costante module-scope:
    // const ENTITY_FLUSH_ORDER = ['tipi_utenza', 'banche', 'proprieta', 'inquilini', 'incassi_affitti', 'utenze'];

    sortQueue(queue) {
      const ORDER = ['tipi_utenza', 'banche', 'proprieta', 'inquilini', 'incassi_affitti', 'utenze'];
      return queue.slice().sort((a, b) => {
        const ai = ORDER.indexOf(a.entity);
        const bi = ORDER.indexOf(b.entity);
        if (ai !== bi) return ai - bi;
        return a.ts - b.ts; // FIFO entro stessa entity
      });
    },

    async enqueueMutation(op, entity, id, payload, baseUpdatedAt = null) {
      const entry = { op, entity, id, payload, ts: Date.now(), attempts: 0, baseUpdatedAt };
      this.mutationQueue.push(entry);
      this._mutationQueueVersion++;
      try {
        if (typeof idbKeyval !== 'undefined') {
          await idbKeyval.set('mutationQueue', this.mutationQueue);
        }
      } catch (e) {
        this.pushErrore({ message: 'enqueue persist: ' + (e?.message || 'unknown'), severity: 'warn' });
      }
      // Trigger immediato se online
      if (this._isOnline) {
        this.flushMutationQueue();
      }
    },

    async applyMutation(entry) {
      // entry = { op, entity, id, payload, baseUpdatedAt, ... }
      // snake_case payload: il caller di enqueueMutation deve già passare snake_case (matcha DDL).
      if (entry.op === 'insert') {
        const { error } = await sb.from(entry.entity).insert(entry.payload);
        if (error) throw Object.assign(new Error(error.message), { status: error.code || error.status });
      } else if (entry.op === 'update') {
        let q = sb.from(entry.entity).update(entry.payload).eq('id', entry.id);
        if (entry.baseUpdatedAt) {
          q = q.eq('updated_at', entry.baseUpdatedAt); // optimistic concurrency — 0 rows = 409
        }
        const { data, error, count } = await q.select('*', { count: 'exact' });
        if (error) throw Object.assign(new Error(error.message), { status: error.code || error.status });
        if (entry.baseUpdatedAt && (!data || data.length === 0)) {
          throw Object.assign(new Error('updated_at mismatch'), { status: 409 });
        }
      } else if (entry.op === 'delete') {
        // soft-delete: set deleted_at; hard-delete via .delete() solo per tipi_utenza
        const { error } = await sb.from(entry.entity).update({ deleted_at: new Date().toISOString() }).eq('id', entry.id);
        if (error) throw Object.assign(new Error(error.message), { status: error.code || error.status });
      } else {
        throw new Error('Unknown op: ' + entry.op);
      }
    },
    ```
  </action>
  <acceptance_criteria>
    - `grep -n "sortQueue(queue)" app.js` ritorna 1
    - `grep -n "enqueueMutation(op, entity" app.js` ritorna 1
    - `grep -n "applyMutation(entry)" app.js` ritorna 1
    - `grep "ENTITY_FLUSH_ORDER\|\['tipi_utenza', 'banche', 'proprieta'" app.js` ritorna ≥ 1
    - `grep "status: 409" app.js` ritorna 1 (throw su mismatch)
    - commit: `feat(pr2b): sortQueue + enqueueMutation + applyMutation helpers`
  </acceptance_criteria>
</task>

### T-05-02-03: `flushMutationQueue()` + listener online + manual flush

<task id="T-05-02-03" wave="3" depends_on="T-05-02-02" type="execute">
  <title>Flush loop con retry/backoff + listener `online` + bottone manuale</title>
  <read_first>
    - .planning/phases/05-pr2b-schema-migration-sync/05-RESEARCH.md §5 (flushMutationQueue skeleton 50 LOC)
    - app.js riga 247 (`online` listener esistente PR1)
  </read_first>
  <action>
    1. Aggiungere `flushMutationQueue` come metodo di `app()`. **Copy verbatim da RESEARCH §5** (skeleton "Flush algoritmo ~50 LOC"), con questi adattamenti finali:
       - Backoff array hardcoded: `[1000, 3000, 9000]`
       - Sui catch branch usare `this.pushErrore({...})` (helper PR1 esistente)
       - Dopo ogni success: `this.lastPushAt = new Date().toISOString();`
       - Reentrancy guard `this._flushing` (boolean)

       Riga chiave verbatim:
       ```js
       async flushMutationQueue() {
         if (!this._isOnline) return;
         if (this._flushing) return;
         this._flushing = true;
         try {
           while (this.mutationQueue.length > 0) {
             const sorted = this.sortQueue(this.mutationQueue);
             const entry = sorted[0];
             try {
               await this.applyMutation(entry);
               const idx = this.mutationQueue.findIndex(e => e.ts === entry.ts && e.id === entry.id && e.entity === entry.entity);
               if (idx >= 0) this.mutationQueue.splice(idx, 1);
               this._mutationQueueVersion++;
               if (typeof idbKeyval !== 'undefined') await idbKeyval.set('mutationQueue', this.mutationQueue);
               this.lastPushAt = new Date().toISOString();
             } catch (e) {
               const status = e?.status || e?.code;
               if (status === 409 || status === '409' || /updated_at mismatch/.test(e?.message || '')) {
                 await this.enqueueConflict(entry, e);
                 const idx = this.mutationQueue.findIndex(x => x.ts === entry.ts && x.id === entry.id && x.entity === entry.entity);
                 if (idx >= 0) this.mutationQueue.splice(idx, 1);
                 this._mutationQueueVersion++;
                 if (typeof idbKeyval !== 'undefined') await idbKeyval.set('mutationQueue', this.mutationQueue);
                 continue;
               }
               if (status === 401 || status === '401' || /jwt|token/i.test(e?.message || '')) {
                 const { error: rErr } = await sb.auth.refreshSession();
                 if (rErr) { this.pushErrore({ message: 'sessione scaduta', severity: 'warn' }); break; }
                 continue;
               }
               if (Number(status) >= 500) {
                 entry.attempts = (entry.attempts || 0) + 1;
                 if (entry.attempts >= 3) {
                   this.pushErrore({ message: `queue: ${entry.entity} ${entry.id} max retries`, severity: 'error' });
                   break;
                 }
                 await new Promise(r => setTimeout(r, [1000, 3000, 9000][entry.attempts - 1]));
                 continue;
               }
               // network / unknown: lascia in coda, esci silently
               this.pushErrore({ message: `queue: ${e?.message || 'unknown'}`, severity: 'warn' });
               break;
             }
           }
         } finally {
           this._flushing = false;
         }
       },
       ```

    2. **Estendere listener `online`** in app.js riga ~247:
       ```js
       window.addEventListener('online', async () => {
         this._isOnline = true;
         if (this.utente) {
           await this.flushMutationQueue();
           await this.caricaDatiUtente();
         }
       });
       window.addEventListener('offline', () => {
         this._isOnline = false;
       });
       ```

    3. **Bottone manuale** "Flush coda manuale" verrà aggiunto in plan 05-03 (Salute dati extension).
  </action>
  <acceptance_criteria>
    - `grep -n "async flushMutationQueue()" app.js` ritorna 1
    - `grep "\\[1000, 3000, 9000\\]" app.js` ritorna 1
    - `grep "await sb.auth.refreshSession()" app.js` ritorna ≥ 1
    - `grep "addEventListener('online'" app.js` ritorna 1 (listener unico, esteso)
    - smoke locale: airplane mode → segna un incasso → vedi entry in `await idbKeyval.get('mutationQueue')` (console); airplane off → entry dispare entro 2s; `app.lastPushAt` valorizzato
    - commit: `feat(pr2b): flushMutationQueue with retry/backoff + online listener`
  </acceptance_criteria>
</task>

### T-05-02-04: `enqueueConflict` + `risolviConflitto` + `nomeUmano` helper

<task id="T-05-02-04" wave="4" depends_on="T-05-02-03" type="execute">
  <title>Conflict queue + resolution logic (binary choose locale|remoto)</title>
  <read_first>
    - .planning/phases/05-pr2b-schema-migration-sync/05-RESEARCH.md §6 (conflict UX + state + helpers)
    - .planning/phases/05-pr2b-schema-migration-sync/05-CONTEXT.md linea 58-65 (locked binary + copy)
  </read_first>
  <action>
    Aggiungere metodi a `app()`:

    ```js
    async enqueueConflict(entry, e) {
      let serverRow = null;
      try {
        const { data } = await sb.from(entry.entity).select('*').eq('id', entry.id).maybeSingle();
        serverRow = data;
      } catch (_) { /* ignore — toast still shows with null serverPayload */ }
      this.conflictQueue.push({
        entity: entry.entity,
        id: entry.id,
        localPayload: entry.payload,
        serverPayload: serverRow,
        nomeEntita: this.nomeUmano(entry.entity, entry.payload),
        ts: Date.now(),
      });
      this._conflictVersion++;
    },

    nomeUmano(entity, payload) {
      // Lookup tabella per "nome utente-visibile" — RESEARCH §6
      if (entity === 'proprieta') return payload?.nome || 'Proprietà';
      if (entity === 'banche') return payload?.nome || 'Banca';
      if (entity === 'inquilini') return payload?.nome || 'Inquilino';
      if (entity === 'tipi_utenza') return payload?.nome || 'Tipo utenza';
      if (entity === 'incassi_affitti') {
        const prop = (this.dati.proprieta || []).find(p => p.id === payload?.proprieta_id);
        return `Incasso ${payload?.mese || ''} su ${prop?.nome || 'proprietà'}`;
      }
      if (entity === 'utenze') {
        const tipo = (this.dati.tipi_utenza || []).find(t => t.id === payload?.tipo_id);
        return `Utenza ${tipo?.nome || ''} ${payload?.fornitore || ''}`.trim();
      }
      return entity;
    },

    async risolviConflitto(scelta) {
      const c = this.conflictQueue.shift();
      this._conflictVersion++;
      if (!c) return;
      try {
        if (scelta === 'mia') {
          // Force update senza updated_at gate
          const payload = { ...c.localPayload, updated_at: new Date().toISOString() };
          delete payload.id; // non aggiornare PK
          await sb.from(c.entity).update(payload).eq('id', c.id);
        } else {
          // Discard local: applica serverPayload allo stato Alpine
          this.applyServerEntity(c.entity, c.serverPayload);
        }
      } catch (e) {
        this.pushErrore({ message: 'risolvi conflitto: ' + (e?.message || 'unknown'), severity: 'error' });
      }
      // Riprendi flush per eventuali altre mutazioni in coda
      this.flushMutationQueue();
    },

    applyServerEntity(entity, serverRow) {
      if (!serverRow) return;
      // Mappa entity name → array chiave in this.dati (camelCase)
      const arrKey = entity === 'incassi_affitti' ? 'incassiAffitti' : entity;
      const arr = this.dati[arrKey];
      if (!Array.isArray(arr)) return;
      // Conversione snake → camel via adattaShape parziale
      const fake = { [entity]: [serverRow] };
      const adapted = this.adattaShape(fake)[arrKey];
      const mapped = adapted && adapted[0];
      if (!mapped) return;
      const idx = arr.findIndex(x => x.id === serverRow.id);
      if (idx >= 0) arr[idx] = mapped; else arr.push(mapped);
    },
    ```
  </action>
  <acceptance_criteria>
    - `grep -n "async enqueueConflict" app.js` ritorna 1
    - `grep -n "nomeUmano(entity, payload)" app.js` ritorna 1
    - `grep -n "async risolviConflitto" app.js` ritorna 1
    - `grep "applyServerEntity" app.js` ritorna ≥ 2 (def + uso)
    - commit: `feat(pr2b): conflict queue + risolviConflitto binary choose`
  </acceptance_criteria>
</task>

### T-05-02-05: Conflict modal HTML + queue indicator in `index.html`

<task id="T-05-02-05" wave="5" depends_on="T-05-02-04" type="execute">
  <title>Modal conflict + toast "N modifiche in coda" in root x-data</title>
  <read_first>
    - .planning/phases/05-pr2b-schema-migration-sync/05-RESEARCH.md §6 (template HTML literal)
    - index.html riga 470 (`<div x-data="app()">` root)
  </read_first>
  <action>
    Inserire dentro `<div x-data="app()">` a root scope (sibling di undo toast / install banner PR2a), subito dopo il banner install:

    1. **Conflict modal** (non-dismissible: no overlay click, no Esc handler):
       ```html
       <!-- Conflict modal (PR2b REQ-SYNC-02) — non-dismissible, FIFO queue -->
       <div x-show="conflittoCorrente()" x-cloak data-testid="conflict-toast"
            class="modal-overlay"
            style="position: fixed; inset: 0; background: rgba(0,0,0,0.45); z-index: var(--z-modal, 9000);
                   display: flex; align-items: center; justify-content: center; padding: 16px;">
         <div class="glass-card" style="max-width: 480px; padding: 24px; display: flex; flex-direction: column; gap: 12px;">
           <h3 style="font-weight: 700; font-size: 18px;">
             <span>⚠</span> Conflitto su <span x-text="conflittoCorrente()?.nomeEntita"></span>
           </h3>
           <p style="font-size: 14px; color: var(--text-secondary);">
             Hai modificato questa <span x-text="conflittoCorrente()?.entity"></span> mentre eri offline.
             Nel frattempo è stata modificata anche dal cloud.
           </p>
           <p style="font-size: 14px;">Quale versione vuoi tenere?</p>
           <div style="display: flex; gap: 12px; margin-top: 8px;">
             <button @click="risolviConflitto('mia')" data-testid="conflict-keep-local" class="btn-primary">
               Usa la mia versione
             </button>
             <button @click="risolviConflitto('cloud')" data-testid="conflict-keep-server" class="btn-secondary">
               Usa versione cloud
             </button>
           </div>
         </div>
       </div>
       ```

    2. **Queue indicator** (top-right toast quando ci sono mutazioni pendenti offline):
       ```html
       <!-- Mutation queue indicator (PR2b REQ-SYNC-01) -->
       <div x-show="mutationQueueCount() > 0 && !_isOnline" x-cloak data-testid="queue-indicator"
            class="glass-card"
            style="position: fixed; top: 16px; right: 16px; padding: 8px 14px; z-index: var(--z-toast);
                   font-size: 13px; font-weight: 500;">
         <span x-text="mutationQueueCount() + ' modifiche in coda'"></span>
       </div>
       ```

    Copy italiano LOCKED da CONTEXT linee 162-167. NON paraphrasare.
  </action>
  <acceptance_criteria>
    - `grep "data-testid=\"conflict-toast\"" index.html` ritorna 1
    - `grep "data-testid=\"conflict-keep-local\"" index.html` ritorna 1
    - `grep "data-testid=\"conflict-keep-server\"" index.html` ritorna 1
    - `grep "data-testid=\"queue-indicator\"" index.html` ritorna 1
    - `grep "Usa la mia versione" index.html` ritorna 1
    - `grep "Usa versione cloud" index.html` ritorna 1
    - `grep "modifiche in coda" index.html` ritorna 1
    - commit: `feat(pr2b): conflict modal + queue indicator HTML`
  </acceptance_criteria>
</task>

### T-05-02-06: `salva()` dual-write branch + entity-aware enqueue wrappers

<task id="T-05-02-06" wave="6" depends_on="T-05-02-05" type="execute">
  <title>`salva()` chiama enqueueMutation per ogni entità modificata + dual-write blob</title>
  <read_first>
    - app.js riga 810-843 (`salva()` esistente) e 846-945 (`salvaSubito()` esistente)
    - .planning/phases/05-pr2b-schema-migration-sync/05-RESEARCH.md §1 touchpoints riga 810/846 + §7 dual-write mechanics
  </read_first>
  <action>
    Strategia dual-write (DEC-012 Phase 1):

    1. **Rinominare** `salvaSubito` esistente in `salvaSubitoBlob` (sed; check tutti i call sites — dovrebbero essere 1-2 dentro `salva()`).

    2. **Riscrivere `salva()`** preservando snapshot/debounce esistente, aggiungendo branch:
       ```js
       async salva() {
         if (!this.utente) return;
         this.statoSalvataggio = 'salvataggio';
         // defensive snapshot prime (carry-over PR2a SNAP-01 — già in place)
         if (!this._lastSnapshotData) {
           try { this._lastSnapshotData = JSON.parse(JSON.stringify(this.dati)); } catch (_) {}
         }
         if (this._lastSnapshotData) this.pushSnapshot(this._lastSnapshotData);

         if (this.usaNuovoSchema) {
           // PR2b: write path per-entità via mutation queue.
           // I form individuali (proprietà/incasso/utenza/banca/inquilino/tipo)
           // chiamano direttamente enqueueMutation con il delta — qui salva() resta
           // il top-level finalizer che (a) bumpa _lastSnapshotData e (b) dual-write blob.
         }

         // Dual-write Phase 1: scrivi anche blob legacy come safety net
         // (skip se offline — solo enqueue per-entity sopravvive a refresh).
         if (this._isOnline) {
           try {
             await this.salvaSubitoBlob();
           } catch (e) {
             this.pushErrore({ message: 'blob fallback: ' + (e?.message || 'unknown'), severity: 'warn' });
           }
         }

         this._lastSnapshotData = JSON.parse(JSON.stringify(this.dati));
         this.statoSalvataggio = 'salvato';
       },
       ```

    3. **NON modificare** i form save handlers ora (proprietà/utenza/banca/incasso). Quelli verranno aggiornati nei plan 05-04 (nuove entità) e nel plan 05-03 (migrazione retrofitting). I form esistenti continuano a fare mutate-in-place su `this.dati`, e `salva()` dual-writes via blob.

       **Eccezione**: aggiornare adesso un solo call site per dogfooding — il form **salva proprietà** (app.js ~riga 1305-1330). Dopo il push to `this.dati.proprieta`, aggiungere:
       ```js
       if (this.usaNuovoSchema) {
         const snake = {
           id: prop.id,
           user_id: this.utente.id,
           nome: prop.nome,
           tipo: prop.tipo,
           importo_affitto_mensile: prop.importoAffittoMensile,
           currency: prop.currency,
           banca_incasso_id: prop.bancaIncasso || null,
           banca_destinazione_id: prop.bancaDestinazione || null,
           intestatario: prop.intestatario,
           scadenza_giorno: String(prop.scadenzaGiorno || '1'),
           note: prop.note || '',
         };
         await this.enqueueMutation(esistente ? 'update' : 'insert', 'proprieta', prop.id, snake, prop._serverUpdatedAt || null);
       }
       ```

       Il pattern viene replicato negli altri form nel plan 05-04. Per `incassi_affitti`, `utenze`, `inquilini` un task dedicato in 05-04.
  </action>
  <acceptance_criteria>
    - `grep "salvaSubitoBlob" app.js` ritorna ≥ 2 (rename completo)
    - `grep "salvaSubito\\b" app.js` ritorna 0 (vecchio nome rimosso)
    - `grep "if (this.usaNuovoSchema)" app.js` ritorna ≥ 2 (in salva() + nel save proprietà)
    - `grep "enqueueMutation.*'proprieta'" app.js` ritorna ≥ 1
    - smoke locale: modifica nome proprietà → console `await idbKeyval.get('mutationQueue')` mostra 1 entry con `entity: 'proprieta'`; tab Network mostra anche `POST /rest/v1/dati_utente` (blob dual-write); ricarica pagina → entry persiste in IDB
    - commit: `feat(pr2b): salva() dual-write branch + proprieta enqueue retrofit`
  </acceptance_criteria>
</task>

## Verification
- DevTools → Application → IndexedDB → `keyval-store` → `keyval` → vedi `mutationQueue` array dopo ogni modifica offline.
- Test manuale 3-write topologico: airplane mode ON → crea proprietà → crea incasso che la referenzia → crea utenza con tipo `acqua` → console `app._mutationQueueDebug()` mostra 3 entry; airplane OFF → entry vengono flushate in ordine `proprieta → incassi_affitti → utenze`; SQL `select * from proprieta` su Supabase mostra il record.
- Test manuale conflict (manual): in console esegui `await sb.from('proprieta').update({ updated_at: new Date(Date.now() + 60000).toISOString() }).eq('id', 'YOUR-ID')` per simulare server-ahead, poi modifica la stessa proprietà offline + online → modal "Conflitto su [nome]" appare → click "Usa cloud" → modal scompare, stato locale = server payload.

## Threat model
- **Migration race condition multi-tab**: 2 tab dello stesso utente leggono entrambi `mutationQueue` al boot, scrivono entrambi su modifica → ultima write vince in IDB. **Mitigation** (R-NEW-4): ack che è basso rischio family-tier mobile single-window; entry "perse" recoverable via snapshot ring di PR1 (il blob legacy dual-write contiene comunque la modifica). Documentato in RESEARCH §4.
- **Auth bypass via stale JWT**: 401 catch → `refreshSession()` retry trasparente. Se refresh fallisce (refresh token expired) → break + toast "Sessione scaduta". L'utente è forzato a re-login; mutation queue persiste in IDB e verrà flushata al prossimo login successo.
- **Conflict modal bypass**: non-dismissible by design — no `@click` su overlay, no `@keydown.escape`. L'utente DEVE cliccare uno dei 2 bottoni. Se ci sono N conflitti in coda, la modal si ri-apre con il successivo dopo ogni risoluzione.
- **Data exfil via cross-user IDB**: `idb-keyval` usa un singolo IndexedDB database `keyval-store` per origin. Se 2 utenti diversi usano lo stesso browser, devono fare login → migration check `tipi_utenza.user_id` filtra correttamente; ma `mutationQueue` IDB residua del primo utente verrebbe **applicata col JWT del secondo** se non clear su logout. **Mitigation**: in `logout()` (app.js esistente) aggiungere `await idbKeyval.del('mutationQueue'); this.mutationQueue = []` — **DA AGGIUNGERE come bonus in T-05-02-03 o estrazione separata in plan 05-03**.

## Risks & mitigations
- **R4 (mutation queue corrompe stato)**: mitigato da FIFO order + 3× retry + persistent indicator. Se queue cresce >100 entry, considerare batch flush.
- **R-NEW-1 (private mode IDB)**: try/catch attorno a tutte le chiamate idbKeyval; fallback in-memory (perde entries su refresh ma no crash). Documentato; non blocker.
- **R-NEW-7 (Alpine reactivity gap su navigator.onLine)**: state slot `_isOnline` + listener `online`/`offline` bumping. Verificare visivamente che indicator scompaia entro 1s dopo "Network online" in DevTools.
- **Logout senza clear queue**: ADD-TODO per plan 05-03 — `logout` deve `idbKeyval.del('mutationQueue')`.

---

*Plan 05-02 of phase 05-pr2b-schema-migration-sync*
